# 投稿卓 NEO — 作業のときの決めごと

## モデルの使い分け

**考えるのは Opus、下請けは Sonnet。** セッションの使用料を下げるための取り決め。

- サブエージェント（Agent ツール）を起動するときは、必ず `model: "sonnet"` を指定する
- 設計・判断・レビュー・利用者への説明は Opus のまま
- 仕様がはっきり決まっていて、成否を機械で確かめられる作業を Sonnet に渡す
  （例：決めた形どおりの実装、テストを書く、調べもの）

`settings.json` にサブエージェントの既定モデルを決めるキーは無いので、
呼び出しのたびに指定するしかない。忘れると Opus で動いてしまう。

## このアプリの決めごと（何度も確認しないで済むように）

1. **嘘を書かない（最重要）**
   運用者はネットの口コミを集めて紹介するキュレーターで、転職の実体験は無い。
   一人称の体験・出典のない数値・効果の断定・実在しない口コミの捏造は禁止。
   **プロンプトに書くだけでは足りない。生成結果を機械で点検して止める**
   （`lib/draft-rules.js`）。テストで担保すること。

2. **追加のお金がかかる実装はしない**
   すでに払っているもの（OpenAI、Supabase、Vercel の無料枠）の中で作る。
   新しく課金が必要になるものは、実装する前に必ず相談する。
   例：X の読み取りAPIは月$200 なので分析の対象から外した。

3. **PR表記**
   案件リンクを含む投稿には PR 表記を入れる（ステマ規制）。
   `#PR` はハッシュタグ側に入れるのが実務なので、点検は「投稿される本文」を見る。

4. **移行ファイルは足すだけ**
   `supabase/schema_vN_*.sql` を新しく作る。既存のファイルは書き換えない。
   足したら `supabase/setup_all.sql` を作り直す（テストが同期を見ている）。
   何度流しても壊れないように書く。

5. **LLM と SNS の呼び出しは `lib/` の中だけ**
   `api/` は入り口の作法だけを持つ。

6. **npm パッケージを増やさない**
   Node の標準機能だけで書く。外部APIは `fetch` で自前で叩く。

## ブランチ

- 作業は `claude/affiliate-auto-post-bot-5nqziz`
- **本番は `claude/posting-desk-neo-cloud-ojt1pj`。マージすると Vercel が本番に反映する**
- 本番ブランチへ直接 push はしない。PR を立ててマージする

## 確かめ方

- `for f in test/*.test.js; do node $f; done`（npm 不要）
- 画面の確認は Chromium（Playwright）。動画の見た目は画素を数えて確かめる
- SQL は Postgres 16 を立てて、まっさらなDBと2回流しの両方を見る
