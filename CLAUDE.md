# このリポジトリについて

「AIアプリ開発初心者が30日で30アプリ作る」企画の **DAY3：チンチロ（NHKチンチロ）** です。`index.html` 1枚で動き、ビルドもサーバーも要りません。テストは `test/` にあります。

この日の新しい挑戦は、**GitHub連携で開発すること**と、**AIに「作って」と言わず先に要件定義をさせること**でした。

企画のルール・ロードマップ・日次ログは管理リポジトリ **[30days-30apps](https://github.com/ainaraomakaseare-coder/30days-30apps)** にまとめます。**開発ログをこのリポジトリに書かないでください。**

> **移行待ち**：管理リポジトリがまだ無いため、その中身を一時的にこのリポジトリの `docs/` に置いてあります。管理リポジトリができたら `docs/` の中身をそちらのルートへ移し、ここからは削除します。この作業は DAY6 の「散らばったアプリの置き場所を整理する」の一部です。

## 開発を手伝うときのお願い

要件が曖昧なら先に質問する／大型なら機能を分割する／初心者に分かる言葉で説明する／エラーやつまずきも記録に残す／費用が発生する場合は着手前に伝える／APIキーや秘密情報をコードやGitHubに出さない。

計測できなかった時間や費用は、推測で埋めずに「正確な計測不可」と書きます。

詳細は `docs/CLAUDE.md`（移行後は管理リポジトリの `CLAUDE.md`）を参照。

## テスト

```
node test/rules.test.js      # 役の判定と点の計算
node test/ui.smoke.js        # 実ブラウザでの画面の流れ（要 playwright）
```
