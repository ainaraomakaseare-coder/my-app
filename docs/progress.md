# 進捗

2026-09-24 観戦日記（旧ベイ日記）を12球団向けの名前にリネーム：アプリ内表示・README・プライバシーポリシー・iOSアプリ化まわりの文言を「観戦日記」に統一し、アプリアイコン・スプラッシュ画面もチケット＋野球の縫い目モチーフ（球団名なし）に作り直した。機能はまだ横浜DeNAベイスターズ専用のまま（名前だけの変更）。詳細は`logs/day23.md`の追記を参照。
2026-09-24 観戦日記 iOSアプリ化：`apps/day23-baydiary-ios/`を追加し、旅の足跡（DAY26）と同じCapacitor＋GitHub Actionsの仕組みでTestFlight配信できるようにした。Apple Developerアカウント・証明書・APIキーは旅の足跡と共有するため追加費用なし。Bundle ID固有のプロビジョニングプロファイルのみ新規に必要（未実施）。ワークフロー自体は未検証。応援球団選択・球場別/月別成績などの機能追加は[観戦日記引き継ぎ](baydiary-handoff.md)のとおり引き続き未着手。詳細は`logs/day23.md`の追記を参照。

2026-09-23 観戦日記（旧ベイ日記） 中断：応援球団を選べるようにする／球場別・月別成績／実際のチーム勝率との比較、という要望が来たところで別セッションに引き継ぎ。詳細・注意点は [観戦日記引き継ぎ](baydiary-handoff.md)。

旅の足跡DAY30完了（30日目、最終日）：「TimeTreeを参考にした、線をなくしたきれいでモダーンな感じにしたい」という要望を受け、Claude Designでホーム画面・旅行詳細の2画面をモックアップし、確認してもらってからアプリ全体の`style.css`・`index.html`を刷新。背景を薄いグレー、カードは枠線を使わず白＋影のみ、フォントをZen Maru Gothicからシステム標準のサンセリフに、アクセントカラーを緑系に変更。確認作業中に、旅行詳細のカバー写真ヘッダーのボタン配置に元々CSSが無かった作り込み漏れも発見・修正した。詳細は`logs/day30.md`。

旅の足跡DAY29完了：DAY28の続き。「保存に失敗しました」という報告の原因が、コードのバグではなくユーザーの手元で`wrangler deploy`前に`git pull`していなかったことだと`wrangler tail`のログから切り分けた。加えて動画サムネイルの粒度統一、宿泊先の泊数別表示、総費用の内訳表示、記録内の複数写真のスワイプ送り、ホーム画面の旅行一覧を誰と一緒か・年・旅行区分で絞り込む機能を追加。詳細は`logs/day29.md`。

旅の足跡DAY28完了：TestFlightで実際に使ってみて溜まった不具合・要望11項目（カメラクラッシュ、動画バグ、評価の0.1単位対応、日タブのスワイプ、天気の手動修正、レシート読み取り、割り勘、訪れた都道府県・国の集計、地図検索など）をすべて回収。開発プロセスのプレイブックを軽量に取り入れ、コミット前のセルフレビューを運用に加えた。詳細は`logs/day28.md`。

ポイまも追加公開：PR #38でデザイン刷新、マイル単位、今日と失効日を表示する月間カレンダーを反映。通常マイルの期限にも対応。テスト231件通過。実画像APIによる認識精度は未検証。

2026-09-18 Codex継続：ポイまもの失効月別読み取りを修正し、PR #36とWorker再デプロイで公開済み。公開HTMLの反映を確認。実画像検証は未実施。再開地点は [ポイまも引き継ぎ](poimamo-handoff.md)。

最終更新：2026-09-23（DAY30 着手・完了。ロードマップの番号は1〜30まで埋まりましたが、DAY8〜9・11〜17・21〜22 はまだ未着手のまま残っています）

## 30日のロードマップ

「新しい挑戦」がその日の動画の芯です。アプリを作ることより、この列を達成することが目的。

| DAY | アプリ | 新しい挑戦 | 状態 | リポジトリ |
|---|---|---|---|---|
| 1 | くじ引き | 初めてAIでアプリを作る／環境構築 | 完了 | 未登録 |
| 2 | ミリオネア | Windows環境で作り直す | 完了 | 未登録 |
| 3 | チンチロ（NHKチンチロ） | GitHub連携／AIに要件定義をさせる | 完了 | my-app `apps/day03-chinchiro/` |
| 4 | 輸入ブラックジャック | 特殊で複雑なルールをAIに理解させる | 完了 | my-app `apps/day04-blackjack/` |
| 5 | ドラマ王 | 外部から情報を取ってくる | 完了 | my-app `apps/day05-dramaou/` |
| 6 | 人狼 | インターネットに公開する／複数端末／**散らばったアプリの置き場所を整理する** | 完了 | my-app `apps/day06-jinro/` |
| 7 | サッカー出欠管理 | 初めてのデータベース（保存が残る） | 完了 | my-app `apps/day07-soccer-attendance/` |
| 8 | 草野球チーム管理 | 初めての大型アプリ（機能分割） | 未着手（以前から着手済み） | — |
| 9 | SNS投稿管理 | 初めてのAPI連携 | 未着手 | — |
| 10 | ドラマ王（ブラッシュアップ） | Skills でAI自体をレベルアップ | 完了 | my-app `apps/day05-dramaou/` |
| 11〜17 | 未定 | 下記の候補から | 未着手 | — |
| 18 | おもいでWiki | 音声で会話する（読み上げ→音声入力）／写真をアプリだけで扱う／サーバーなしで複数人の記録を1つに合体する | 完了 | my-app `apps/day18-omoide-wiki/` |
| 19 | おもいでWiki（ブラッシュアップ） | 音声×AIの深掘りインタビュー（回答に応じてAIがその場で追加質問を作る） | 完了 | my-app `apps/day18-omoide-wiki/` |
| 20 | 旅の足跡 | 音声認識（録音→文字起こし→AIによる記録の自動整理）／ログイン（Google・Apple・メールOTPでの本人確認） | 完了 | my-app `apps/day07-tabilog/` |
| 21〜22 | 未定 | 下記の候補から | 未定 | — |
| 23 | 観戦日記（旧ベイ日記） | 個人的な観戦記録から選手の活躍を分析する | 完了 | my-app `apps/day23-baydiary/` |
| 24 | ポイまも | スクリーンショットをAI（画像理解）に読み取らせて登録内容を自動入力する | 完了（Worker未デプロイのためAI機能は未有効化） | my-app `apps/day24-poimamo/` |
| 25 | ポイまも（デザイン刷新＋内訳更新） | Claude Designで複数のビジュアル案を出して選んでもらう／1サービスが複数の失効日内訳を持てる構造にし、スクショでサービス単位に一括更新する | 完了（2026-09-18にWorkerをデプロイし、AI機能も有効化済み） | my-app `apps/day24-poimamo/` |
| 26 | 旅の足跡（有料サブスク＋App Store配信） | 初めての有料サブスク課金（Stripe）の実装／初めてのiOSアプリのストア配信（TestFlight） | 完了（TestFlight配信済み。Stripeはテストモードのみ、App Store本審査は未提出） | my-app `apps/day07-tabilog/` |
| 27 | ポイまも（複数スクショ読み取り＋カレンダー連携） | スクショを複数枚まとめてAIに読み取らせる／内訳ごとにカレンダー（.ics）へリマインドを登録する | 完了 | my-app `apps/day24-poimamo/` |
| 28 | 旅の足跡（バックログ機能追加・不具合修正） | 運用フィードバック（実際に使ってみて出た不具合・要望）から1件ずつ改善するサイクル | 完了 | my-app `apps/day07-tabilog/` |
| 29 | 旅の足跡（DAY28の続き・デプロイ運用の罠と追加機能） | 「保存に失敗しました」の原因がコードではなくデプロイ前のgit pull忘れだったと切り分けた／宿泊先・総費用内訳・写真スワイプ・旅行一覧の絞り込みを追加 | 完了 | my-app `apps/day07-tabilog/` |
| 30 | 旅の足跡（デザイン刷新・TimeTree風） | Claude Designでモックアップを先に確認してもらってから、アプリ全体のCSSを「線を使わない・TimeTree風モダン」に刷新 | 完了 | my-app `apps/day07-tabilog/` |

DAY7 は2026-08-27に「サッカー出欠管理」として開発していましたが、当時はリポジトリを分ける方針だったため別リポジトリ [`day07-soccer-attendance`](https://github.com/ainaraomakaseare-coder/day07-soccer-attendance) に置いたまま、このリポジトリに合流していませんでした。2026-09-16 に `apps/day07-soccer-attendance/` として取り込み、トップページにも並べました。あわせて、番号を保留にしたまま `apps/day07-tabilog/` に入っていた「旅の足跡」の表示を **DAY20** に直しています（フォルダ名は公開URLが変わるためそのままです）。旅の足跡の実装の経緯は `logs/tabilog-implementation.md` に残しました。

DAY10 は当初「くじ引きリメイク」の予定でしたが、**題材を DAY5 のドラマ王のブラッシュアップに変えました**。新しい挑戦「Skills でAI自体をレベルアップ」は変えていません。既に動いているアプリを磨くほうが、Skills を入れる前と後の差が見えると判断したためです。DAY8〜9・11〜17 は未着手のまま残っています。

DAY18 は「ひとりの人生や、サークル・チームの思い出を、質問で深掘りしながらWikipedia風にまとめるアプリ」というリクエストから始めました。書く負担を減らしたいという要望を受けて、音声入力・読み上げを当日中に追加しています。

DAY19 は DAY18 の続きです。導入した `interview-me` Skill の型（仮説→確信度→一問ずつ質問→確定）でユーザーと要件をヒアリングし、「固定質問だけでは出てこない気持ちや価値観を、AIが回答に応じてその場で追加質問することで引き出したい」「音声でスピーディーに答えたい」という要望を確定させてから実装しました。あわせて、サークル旅行のエピソードをアプリ内で「旅行」単位に整理する機能も追加しています。アプリは分割せず、DAY18と同じ `apps/day18-omoide-wiki/` を育てる形を選びました（データを1箇所にまとめたいという要望のため）。車内トークの録音・書き起こしはDAY20に持ち越しています。要件メモは `apps/day18-omoide-wiki/requirements-day19-20.md` を参照。

DAY20 は「車内トークの録音・書き起こし」「iOSアプリ化の検討」を予定していた枠でした。実際には、DAY7で作っている「旅の足跡」にログイン機能（Google/Apple/メールOTP）と音声認識機能（録音→文字起こし→AIによる記録の自動整理）を追加する形で、この枠にちょうど当てはまる内容を実装しました。新しいアプリは作らず`apps/day07-tabilog/`を育てています。ログインはiOSアプリ化の検討（GoogleログインがiOSアプリ内WebViewで動かない制約の発見）がきっかけで、音声認識は当初「音声を直接AIに聞かせる」設計だったが実機でモデルが非対応と判明し、Whisperでの文字起こし→テキスト構造化という2段階に作り直しています。詳細は`logs/day20.md`。

DAY23 は「野球観戦した試合を記録するベイ日記（横浜DeNAベイスターズ）を作りたい」というリクエストから始めました（後日、12球団のファンでも使える名前という趣旨で「観戦日記」にリネーム。機能はまだベイスターズ専用のままで、名前だけの変更です）。試合日・対戦相手・スコア・勝敗に加えて、同行者・チケット代・天気・写真・自分なりのMVPや興奮したシーンを記録でき、年度別の観戦成績（勝敗・勝率）と、よく活躍を書き留めた選手のランキング分析を自動集計します。試合結果の外部API取得は、安定・無料でNPBデータを返すAPIが見当たらなかったため、`api-sports.io`の任意キーを使った試験的機能とし、失敗時は常に手入力にフォールバックする設計にしました。**このDAYでは、開発環境のリポジトリが「30日で30アプリ」構成へ整理される前の古い状態のまま残っていたことに気づかず作業を始めてしまい、アプリをリポジトリ直下に配置してPRを出した後になって`apps/dayNN-アプリ名/`構成・`docs/logs/`のルールに気づき、`apps/day23-baydiary/`への移動とREADME・ロードマップの修正をやり直しました**。詳細は`logs/day23.md`。

DAY24 は「楽天ポイント・Vポイント・ANAマイレージ・Amazonポイントなど、いろんなサービスのポイントの有効期限を管理したい。期間限定ポイントがどれで、いつまで有効かを自動で取得できると嬉しい。自動でできない場合はスクショを取ったら理解して登録しておいてほしい」というリクエストから始めました。着手前に、要件が曖昧な2点（各サービスへの自動ログイン取得の可否と、スクショ読み取りの方式）をユーザーに確認しています。自動ログイン（ID・パスワードを保存して各サービスから直接取得）は、規約違反・セキュリティ・サイト変更で壊れやすいリスクが大きいため**作らない**ことを確認したうえで、手入力による登録・失効日の近い順の並び替え・失効間近の色分け表示を実装しました。スクショ読み取りは、ブラウザ内OCRだと日本語の精度が不安定なため、AI（画像理解）に読み取らせる方式を選び、DAY05・DAY18と同じ「APIキーをブラウザに出さないCloudflare Worker」構成で用意しています。ただし本セッションにはCloudflareへのデプロイ権限が無いため、Workerのコードとデプロイ手順は用意した一方、実際の`wrangler secret put`・`wrangler deploy`はユーザー側での実施が必要です（未デプロイでも手入力機能は問題なく使えます）。AIの読み取り結果は自動保存せず、必ずフォームで確認してから保存する設計にしています。詳細は`logs/day24.md`。

DAY25 は DAY24（ポイまも）の続きです。まず、DAY24の見た目が「ダサすぎる」との評価を受け、Claude Design（デザインキャンバス機能）で3方向のビジュアル案（A. チケット／B. ダークウォレット／C. エディトリアル・レシート）を作成してユーザーに選んでもらい、選ばれたA案（チケット：ミシン目つきの券面、モノスペース数字、暖色の紙背景）をアプリ本体に反映しました。次に、「楽天ポイントには8/30失効の期間限定100pt、9/30失効の期間限定10pt、失効しない通常ポイント1000ptが同時に存在する」という具体例から、「1サービス＝1件」だった前提が実態に合っていないことに気づき、1サービスが複数の内訳（lot）を持てるデータ構造に変更しました。サービスをタップすると内訳一覧の詳細画面に遷移し、そこから「スクショで情報を更新する」を使うと、AIがスクショから複数の内訳を読み取って一覧に入力し、確認・修正した上で確定するとそのサービスの内訳が丸ごと置き換わる仕組みを追加しています（新しいサービスの追加は引き続き1件ずつの追加で、既存データを消さない設計）。AI Workerの抽出スキーマも「1件のポイント情報」から「内訳の配列」に変更しました。詳細は`logs/day25.md`。

DAY26 は DAY20（旅の足跡）の続きです。ログイン・音声認識までがDAY20、そこに有料プラン（Stripeでのサブスク決済）とiOSアプリとしてのTestFlight配信を積み増しました。Stripe実装はCloudflare WorkerからREST APIを直接呼ぶ形（npm SDKなし）で、Checkout・Webhook・解約用のカスタマーポータルまで一通り作っています。iOS側は、実機UDID未登録が原因だった署名エラーを解消してTestFlightアップロードに初成功させた後も、CapacitorのWKWebView特有の制約（クロスオリジンPOSTがCORSで止まる、外部ドメインへの遷移がSafariに逃げる、入力欄タップ時の強制ズーム）を1つずつ切り分けて解決しました。途中、Stripeアカウント側の仕様変更（Managed Payments）や、ユーザーのPCが違うgitブランチのままデプロイし続けていたという、コード以外の原因にも複数遭遇しています。App Storeでの一般公開に向けては、iOSアプリ内でStripe決済を完結させる形がAppleの審査ガイドライン（3.1.1）に抵触しうるという新たな論点も見つかり、次回への持ち越しにしています。詳細は`logs/day26.md`。

DAY27 は DAY24〜25（ポイまも）の続きです。実際に運用してみたところ「通算ポイントが内訳に混入する」「失効月ごとの内訳を読み落とす」という精度課題があり、プロンプトの文言修正だけでは直りきらなかったため、そもそも1枚の画像しか渡せない設計を見直しました。スクリーンショットを複数枚まとめて選択できるようにし、1回のAIリクエストで全ての画像を渡して内訳をまとめて読み取らせる仕組みに変更しています（1画面に収まらない長い内訳一覧も、スクロールしながら複数枚撮れば漏れなく取り込める）。あわせて、「新しいサービスをスクショから登録する」フローと「既存サービスの内訳をスクショで一括更新する」フローを同じレビュー画面に統合し、コードの二重管理を解消しました。さらに、失効日を忘れないよう内訳ごとに「カレンダーに追加（.ics）」ボタンを追加し、失効3日前に通知が来るリマインダーを組み込んでいます。途中、AnthropicのAPIキーが「ワークスペースに紐づいていない」ことが原因の400エラーにも遭遇し、`wrangler tail`でエラー本文が見えるようログを強化したことで原因を特定できました。詳細は`logs/day27.md`。

DAY28 は DAY26（旅の足跡）の続きです。TestFlightで実際にアプリを使い始めたことで溜まった不具合・要望をInstagramのメモにまとめた11項目、優先度を付けずに1件ずつ「回収」する運用フィードバック起点の開発サイクルで対応しました。カメラ起動クラッシュ・動画関連の不具合修正から始まり、記録の時間／その他URL欄、評価の0.1単位対応、日タブのスワイプ、天気の手動修正、レシート読み取り（AI/Vision、着手前に費用感を共有してから実装）、割り勘機能（立て替えた人・割る人を明示的に選ぶ設計、Entryのauthorからの自動推測はしない）、訪れた都道府県・国の自動集計、地図検索（APIキー不要な形式だけを使用）まで、幅広い機能を追加しています。並行して、Apple審査（Guideline 2.1 Information Needed）への対応や、アカウント削除の不正利用対策（新規登録特典・無料プランの利用回数が削除→再登録で復活してしまう穴を発見・修正）も行いました。途中、ユーザーから重量級の開発プロセス（Vモデル式の設計書ゲート・層別TDD）のプレイブックが共有されましたが、アプリの規模（単一ファイルの素のJS＋1つのWorker）には過剰と判断し、「ロジックの多い機能は先にテストを書く」「コミット前にセルフレビューする」の2点だけを軽量に取り入れました。実際にこの運用で、割り勘機能のUIが狭い画面で崩れる不具合をコミット前に発見・修正しています。詳細は`logs/day28.md`。

DAY29 は DAY28（旅の足跡）の続きです。「評価の保存に失敗しました」「天気の手動修正が保存できない」という報告を何度もの再デプロイ越しに追いかけたところ、コードのバグではなく**ユーザーの手元環境で`wrangler deploy`の前に`git pull`をしていなかったこと**（4日前のコードをデプロイし続けていた）が原因だったと判明しました。`wrangler tail`の「Ok」表示が実は400/404エラーも含む、という見落としにも気づき、教訓を`CONTEXT.md`に明文化しています。あわせて、動画サムネイルの粒度を写真と統一、宿泊先の泊数別表示、総費用のタップでの参加者別内訳表示、記録内の複数写真をライトボックス内でスワイプ送りできる機能、ホーム画面の旅行一覧を誰と一緒か・年・旅行区分（自由入力）で絞り込む機能を追加しました。途中、新機能が「見えない」という報告がブラウザではなくTestFlight（stale化した過去のビルド）での確認だったことも判明し、以後「TestFlightかブラウザか」を先に確認する運用に変えています。また、以前の「÷人数」電卓廃止に回帰テスト（`test/ui.smoke.js`）が追随できておらず壊れていたのも見つけて直しました。詳細は`logs/day29.md`。

DAY30 は DAY29（旅の足跡）の続きです。「TimeTreeを参考にした、線をなくしたきれいでモダーンな感じにしたい」というデザイン刷新の要望を受け、いきなりコードを書き換えるのではなく、Claude Design（Artifactのデザインキャンバス）で実際のアプリの構造（ホーム画面の旅行一覧、旅行詳細のカバー写真・統計カード・日タブ・タイムライン）に沿った2画面のモックアップを作成し、ユーザーに確認してもらってから反映する進め方を取りました（DAY25のポイまもデザイン刷新で確立した型の踏襲）。承認後、`style.css`全体を書き換え、背景を薄いグレー、カードは枠線を使わず白＋控えめな影のみ、フォントをZen Maru Gothicからシステム標準のサンセリフに、アクセントカラーをコーラルオレンジから緑系に変更しました。入力欄は「白いカードの上に白い入力欄では区別が付かない」問題を避けるため、カードの外（ログイン画面・絞り込みプルダウン）は白地＋影、カードの中（記録編集フォームなど）は薄いグレー塗り、と使い分けています。見た目をPlaywrightで確認している途中、旅行詳細のカバー写真ヘッダー（戻る・共有・編集ボタン）に元々レイアウト用のCSSが無く、サムネイル画像の有無にかかわらずボタンが不格好に積み重なっていた、今回のデザイン変更以前からの作り込み漏れを発見し、承認されたモックアップどおりの重ね表示を`:has()`セレクタだけでJSを触らずに実装しました。詳細は`logs/day30.md`。

**DAY11以降の候補（DAY18・19・20・23・24・25・26・27・28以外）**：LP作成（作る→公開する→売る）／野球期待値計算（大量データから正しく取得）／オンライン対戦ゲーム（リアルタイム通信）／iOS・Android のストア提出。詳細は `handoff.md` の14章。

**まだ配置していないつまずき**：モデルの使い分け／他サイトのUIを参考にする。

「セッション利用上限で止まる」は DAY6 で実際に発生したため、`logs/day06.md` に記録済みです。あわせて「人間の判断待ちで作業が止まる」も DAY6 で発生しました。

## リポジトリの決めごと

**DAY6 で方針を変えました。** 当初は「アプリはひとつずつ別のリポジトリに作る」でしたが、作業環境の権限で新規リポジトリを作成できなかったため（`403 Resource not accessible by integration`）、**1つのリポジトリ `my-app` の中で `apps/dayNN-アプリ名/` に分ける**形にしました。

```
my-app/
  index.html            全アプリへのトップページ
  apps/dayNN-アプリ名/  各日のアプリ（1アプリ1フォルダ）
  docs/                 企画のルール・ロードマップ・日次ログ
```

記録は今までどおり `docs/logs/dayNN.md` に集約します。アプリのフォルダには置きません。

将来リポジトリを分けられるようになったときのために `templates/app-repo-CLAUDE.md` は残してあります。分けるときは各リポジトリの `CLAUDE.md` として置いてください。

DAY1（くじ引き）と DAY2（ミリオネア）はまだ入っていません。手元にコードがあれば `apps/day01-kujibiki/` `apps/day02-millionaire/` として追加してください。

## 累計

DAY30 の集計用。埋まっていない欄は**推測で埋めない**。

### 時間

| DAY | 人間 | AI実稼働 | 備考 |
|---|---|---|---|
| 1 | 2h30m | 30m | 17:00〜20:00 |
| 2 | 正確な計測不可 | 正確な計測不可 | 環境構築に大半、アプリ生成自体は速かった |
| 3 | 正確な計測不可 | 正確な計測不可 | |
| 4 | 未記入 | 未記入 | 開発済み。ログをさかのぼって記入する |
| 5 | 未記入 | 未記入 | 開発済み。ログをさかのぼって記入する |
| 6 | 正確な計測不可 | 正確な計測不可 | 実時間で7h25m（23:43〜翌07:08）。中断をまたいだため内訳は取れていない |
| 7 | 0h30m | 正確な計測不可 | 実作業30m。完了まで10時間以上（利用上限2回）。自動計測が発火せず、出た数字も実態と不一致 |
| 8〜9 | 未着手 | 未着手 | 飛ばして DAY10 を先にやった |
| 10 | 正確な計測不可 | 正確な計測不可 | 13:09〜14:05。人間とAIの内訳は分けて計測できていない |
| 11〜17 | 未着手 | 未着手 | |
| 18 | 1h | 2h | DAY18・19を1本の動画としてまとめて公開する方針のため、両日ぶんを合算して本人申告（概算）で計上。開発自体はクラウドの自動実行セッションで、公開後のフィードバック対応・デプロイ作業・テスト確認などを含めた実感値 |
| 19 | ― | ― | DAY18の行に合算（上記参照） |
| 20 | 3h0m | 7h0m | 本人申告。開始・終了時刻はDAY7の作業と地続きのため切り出せず「正確な計測不可」 |
| 23 | 1h30m | 2h0m | 本人申告（概算）。開始・終了時刻は未記録のため「正確な計測不可」。デザイン刷新（Claude Design）とAPI-BASEBALLのプラン制限調査を含む |
| 24 | 正確な計測不可 | 7分 | 14:32〜14:39（UTC）。人間の作業は要件確認の質問1回への回答待ちのみで、待ち時間自体は分離して計測できていない |
| 25 | 正確な計測不可 | 11分 | 03:06〜03:17（UTC）。DAY24の続き（デザイン刷新・データ構造変更）。人間の作業はデザイン案の選択・追加要望の記入のみで、待ち時間自体は分離して計測できていない |
| 26 | 正確な計測不可 | 正確な計測不可 | 複数セッションにまたがるため計測不可。iOSアプリのビルド（GitHub Actions）だけで17回実行している |
| 27 | 正確な計測不可 | 正確な計測不可 | DAY25のデプロイ作業と地続きの1つの会話の中で進めたため、開発だけの時間を切り出せず |
| **合計** | **8h30m + 未計測9日** | **11h48m + 未計測8日** | |

### 費用

| 項目 | 金額 | DAY |
|---|---|---|
| Chromebook | 64,900円 | 1 |
| Claude Code | 19ドル | 1 |
| Windows PC | 129,800円 | 2 |
| 公開（GitHub Pages） | 0円 | 6 |
| OpenAI APIプリペイド | 5ドル | 10 |
| **合計** | **194,700円 ＋ 24ドル** | |

DAY20のOpenAI API消費（音声機能）も、DAY10で購入した同じプリペイド残高から消費しています。新規の課金・購入はありません。

**DAY10でOpenAI APIへ5ドルをプリペイド購入しました。** 実際のAPI消費額はUsage Dashboard反映後でないと正確に分からないため、購入額と消費額を分けて管理します。自動チャージはOFFです。Cloudflare WorkerにAPIキーをsecretとして保存し、24時間キャッシュとレート制限を設定しました。

**DAY18・19（おもいでWiki）でも同じプリペイド残高からOpenAI APIを消費しています。** 正確な消費額はUsage Dashboard確認が必要ですが、呼び出し回数・トークン量からの試算では数十円程度（多くても100円未満）とみられます。新規の課金・購入は発生していません（既存の5ドル残高の消費のみ）。

**DAY24（ポイまも）では新規の課金・購入は発生していません。** AI読み取り用のCloudflare Workerは用意しましたが、本セッションにはデプロイ権限が無いためユーザー側での`wrangler deploy`が必要です。デプロイし、Anthropic（Claude）のAPIキーを登録した時点から初めて費用が発生します（OpenAIとは別のAPIキー・別の課金先）。

**DAY25でも新規の課金・購入は発生していません。** Claude Designでのデザイン案の作成はこのセッション自体のClaude利用に含まれます（個別の追加課金ではありません）。Worker未デプロイの状態は変わっていません。

**DAY23（観戦日記、旧ベイ日記）でも新規の課金・購入は発生していません。** 試合結果の自動取得を試すためapi-sports.io（API-BASEBALL）の無料アカウントを作成しましたが、無料プランの制限（チーム情報は2022〜2024年シーズンのみ、試合結果は実行日前後のみ）が判明し、有料プラン（PRO Plan 月$15〜）への加入は見送りました。

**2026-09-18、DAY25の続きとしてWorkerを実際にデプロイしました。** Cloudflareのアカウント作成・APIトークン発行・Workerのデプロイ自体は無料です。Anthropicの新しいAPIキーを発行しましたが、これは「発行」自体には課金が発生せず、実際にスクショ読み取り機能が使われた分だけ従量課金されます（1回あたりの目安は本文中の会話で概算済み：数百円未満／100回あたり）。消費額はAnthropicのUsage Dashboardで確認可能です。新規のハードウェア購入等はありません。

**DAY26でも新規の課金・購入は発生していません。** Stripeはテストモード（サンドボックス）のみで実装・確認しており、実際の決済は発生していません。Apple Developer Programは年額契約ですが、このセッションで新規に登録したものではありません（以前のセッションで登録済み）。

ドル建ての費用は換算レートを勝手に決めず、そのまま持っておいて最終日にまとめて換算します。

### できるようになったこと（累計）

- Claude Code のセットアップ（Linux / Windows）
- Claude Code でアプリを作る
- 分からない画面をスクショで見せて状況を理解してもらう
- GitHub と連携してデスクトップ環境から開発する
- AI に要件定義をさせる（先に質問させて要件を固める）
- 特殊で複雑なゲームルールをAIに実装させる（DAY4）
- アプリの外にある情報を取ってきて使う（DAY5）
- 作ったアプリをインターネットに公開する（DAY6）
- 複数の端末から同じアプリを使う仕組みを作る（DAY6）
- サーバーを使わずに端末ごとに違う情報を届ける（DAY6）
- 外部ライブラリに頼らず必要な部品を自分で実装する（DAY6）
- 「動いているように見えるが壊れている」ものを機械的に検証する（DAY6）
- 公開前に秘密情報が混ざっていないか検査する（DAY6）
- データベースを使い、閉じても・別端末からでも内容が残るアプリを作る（DAY7）
- テーブルに鍵をかけ（RLS）、合言葉を確かめる関数だけを窓口にする（DAY7）
- 「キーが公開されても平気」な仕組みを、テストで実際に確かめる（DAY7）
- 判断の基準を Skill として書き出し、次に作るときも同じ基準を効かせる（DAY10）
- 書き出した基準を自分の作ったものに当てて、見えていなかった不具合を見つける（DAY10）
- 記事の文章から、事実だけでなく「物語の中身」を問う問題を組み立てる（DAY10）
- 作ったものを機械で検査し、落ちたぶんは直さず捨てる（DAY10）
- 課金の起きる作りに気づいて、お金のかからない作りに設計し直す（DAY10）
- 自動では作れない問題を見きわめて、そこだけ手で用意する（DAY10）
- APIキーを公開HTMLへ置かず、Cloudflare Workerのsecretで管理する（DAY10）
- 外部情報をAIへ渡し、候補を自己レビューさせて物語問題10問を生成する（DAY10）
- キャッシュとレート制限で有料APIの費用を守る（DAY10）
- ブラウザ標準の音声認識・読み上げ（Web Speech API）で、外部APIなしの音声会話を作る（DAY18）
- 写真をアップロード時に自分で縮小・圧縮し、サーバーを使わずアプリの中だけで扱う（DAY18）
- サーバーを立てずに、複数人がバラバラに書いた記録をJSONの書き出し・読み込みで1つに合体させる（DAY18）
- 「要件が曖昧なら先に質問する」を実践しつつ、対話の中で出てきた追加要望（音声対応）をその場で拾って仕様に組み込む（DAY18）
- `interview-me` Skillの型（仮説→確信度→一問ずつ質問→確定）で、大きく育った要望を1つずつ言語化してから実装に入る（DAY19）
- 個人的な回答をAIに読ませて、その場で自然な追加質問を1つだけ作らせる（キャッシュはせず、深掘りの回数に上限を設けて費用を抑える）（DAY19）
- 音声の読み上げ→音声入力のループに、AIが動的に作る質問をそのまま乗せる（決め打ちの質問リストと同じ画面遷移で、動的な質問も扱えるように設計する）（DAY19）
- 機能を作る前に「アプリを分けるか／1つにまとめるか」を判断し、データを1箇所に保つ理由を言葉にしてから決める（DAY19）
- ログイン機能を作る際、実行環境（ブラウザ／アプリ内蔵WebView）ごとの制約の違い（GoogleのOAuthがWebViewでブロックされる）に気づき、設計を作り直す（DAY20）
- 一度「要件が固まっていないため見送り」とした機能を、具体例が出て像が掴めたタイミングで`interview-me`から再開する（DAY20）
- 音声入力を「文字起こしをそのまま貼るだけ」ではなく「複数の記録への分割・分類までAIにやらせる」設計にしつつ、AIが誤りやすい情報（評価・費用・日付）は今回のスコープに含めないと線引きする（DAY20）
- `wrangler tail`でリアルタイムログを見ながら、CORS不具合→AI呼び出し自体の不具合、と段階的に原因を切り分けて直す（DAY20）
- 机上の設計では気づけない、実機・実際のAPIで動かして初めて分かる制約（WebViewでのOAuthブロック、音声モデルの非対応）があることを実感する（DAY20）
- 「自動化できると嬉しい」という要望でも、実現手段ごとにリスク・コストが大きく違うことに気づき、着手前にどちらにするかユーザーに確認する（DAY24）
- スクリーンショット画像をAIのtool use（構造化ツール）に読み取らせ、自由な文章ではなく決まった項目だけを返させる（DAY24）
- AIの読み取り結果を自動保存せず、必ず人が確認してから保存するフォールバック設計にする（DAY24）
- デプロイ権限が無い開発環境でも、コードとデプロイ手順を用意して人間側の作業に引き継ぐ（DAY24）
- Claude Design（デザインキャンバス）で複数のビジュアル方向性を並べて見せ、ユーザーに選んでもらってから実装に反映する（DAY25）
- 「1件のレコードが1つのサービスに対応する」という当初のデータ構造の前提を、ユーザーの具体例をきっかけに「1サービスが複数の内訳を持てる」構造へ設計し直す（DAY25）
- スクショ1枚から複数の値をまとめて読み取らせ、レビュー画面で確認・修正してから一括で確定させる（DAY25）
- 精度が上がらないとき、プロンプトの微調整で粘るのではなく「1枚しか画像を渡せない」という設計上の制約自体を疑い、複数画像対応に作り直す（DAY27）
- 似た役割の2つの画面（新規登録・一括更新）を条件分岐で1つのコンポーネントに統合し、コードの二重管理を避ける（DAY27）
- `.ics`ファイルをその場で生成してダウンロードさせ、外部のカレンダーアプリにリマインダーとして連携する（DAY27）
- APIキーが「組織スコープ」か「ワークスペーススコープ」かで動作が変わるという、Anthropic APIの実運用上の制約に気づく（DAY27）
- Cloudflare WorkerからStripeのREST APIを直接呼び、サブスク決済・Webhook処理・解約用カスタマーポータルまでを実装する（DAY26）
- `wrangler tail`でOriginヘッダーやレスポンスステータスを見ながら、Capacitor側の制約とStripe側の仕様変更という別々の原因を切り分ける（DAY26）
- CapacitorのWKWebViewが持つ複数の既知の制約（クロスオリジンPOSTがCORSで止まる、外部ドメインへの遷移がSafariに逃げる、16px未満のinputでタップ時に強制ズームする）を、それぞれ別の仕組みで解決する（DAY26）
- 同じ不具合が直らない原因が、コードではなく開発環境側（デプロイ元のgitブランチが違う）にあることを、実際の挙動（デプロイのたびに反映される内容が変わらない）から疑って特定する（DAY26）
- Apple Developerでの実機UDID登録から、GitHub ActionsでのiOSアプリ自動署名・TestFlightアップロードまでの一連の流れを組み立てる（DAY26）
- アプリ内で決済を完結させる実装が、便利になった一方でApple審査ガイドライン（3.1.1）上のリスクを新たに生むことに気づき、安全な代替パターンを整理する（DAY26）

## DAY6 でやった片付け（完了）

5日分のアプリの置き場所が崩れていたのを整理しました。

**やったこと**

- 合流していないブランチに積み上がっていたアプリを `apps/` 以下へ集約した
- `main` ブランチを作った。「合流していないブランチだけ」の状態が解消された
- トップページ `index.html` から全アプリへ辿れるようにした
- 各アプリのテストのパスを新しい場所に合わせ、**移動後も全て通ることを確認した**（合計1028件）
- 公開前に全ブランチをAPIキー混入で検査した（検出なし）
- GitHub Pages で公開した

| 移動元ブランチ | 移動先 |
|---|---|
| `claude/consultation-f97x8e` | `apps/day03-chinchiro/` |
| `claude/blackjack-game-dev-1wvcdm` | `apps/day04-blackjack/` |
| `claude/drama-quiz-app-7kzkkq` | `apps/day05-dramaou/` |
| `claude/30days-30apps-handoff-q8l4k9` | `docs/` |

**予定と変わったところ**

管理リポジトリ `30days-30apps` を作る予定でしたが、権限が無く作成できませんでした（403）。リポジトリ分割はあきらめ、1リポジトリ内での整理に切り替えています。

**まだ残っていること**

- 古い `claude/*` ブランチはまだ消していません。`main` が問題なく動くことをしばらく確認してから消してください（消すのは最後、の方針どおり）
- DAY1・DAY2 のコードが手元にあれば追加する
- DAY4・DAY5 のログをさかのぼって書く

## リポジトリに入れた Skill（DAY10）

`.claude/skills/quiz-design/` に「クイズの出題設計」を Skill として置きました。**Claude がクイズの話をしているときに自動で読みます。** 中身は、出題を出す前の4つの検査・ダミー選択肢の作り方・難易度の層の持ち方・AIに問題を作らせるときの作法と、検査を機械で回すスクリプトです。

DAY2 のミリオネアも DAY5 のドラマ王もクイズなので、この先クイズを作るたびに効きます。実際、DAY10 でこの Skill をドラマ王に当てたところ、**同じ言い回しの設問が4問並んでいたこと**と、**内訳の帯が一度も伸びていなかったこと**が見つかりました。Skill には、今回うまくいった「AIを使わずに地の文から中身を問う」やり方も書き足してあります。

## 公開先

https://ainaraomakaseare-coder.github.io/my-app/

GitHub Pages（`main` ブランチのルート）。パブリックリポジトリなので**費用は0円**です。

## 記録の場所

各日の詳細は `logs/dayNN.md`。開発開始時に `logs/TEMPLATE.md` をコピーして開始時刻から書き始め、終了時にこのファイルの表も更新します。


### DAY23 追記（2026-09-24）

ベイ日記をNPB12球団対応へ拡張。今季・通算・直近6試合、年度別・月別・球場別、手入力のチーム勝率比較を追加し、デザインを刷新。詳細分析はPlusプレビューとして無料で提供し、決済は未導入。ロジック43件・既存UI27件・分析UI46件を検証。詳細は `logs/day23.md`。

DAY23 追加改修：成績の画像共有と、過去メモ→AI構造化JSON→確認付き追加を実装。既存116件に新規50件を加え166件検証。OpenAIキー設定と実サービスでの読取確認は未実施。公開iOS向けバックエンド・決済は今回の対象外。

DAY23 大会区分追加：交流戦・CS・日本シリーズの独立した成績枠、一覧・分析・共有の大会フィルタ、AI取り込みの大会区分を追加。公式戦には交流戦を含め、CSと日本シリーズは別集計。累計198件の検証。
