# DAY 23：ベイ日記

**新しい挑戦**：個人的な観戦記録から選手の活躍を分析する

## 時間

| | |
|---|---|
| 開発開始 | 正確な計測不可 |
| 開発終了 | 正確な計測不可 |
| 人間作業 | 1h30m |
| AI実稼働 | 2h0m |
| AI待機 | 正確な計測不可 |

開始・終了の時刻自体は記録できていないが、人間作業1h30m・AI実稼働2h0mは本人申告（概算）。セッションの正確な開始・終了時刻をログに残す仕組みがまだないため、時刻そのものは計測できていません。

## 使用モデル

| モデル | 作業内容 |
|---|---|
| Claude Sonnet 5 | 要件確認、`apps/day23-baydiary/`（旧`bay-diary.html`）の実装・テスト作成、リポジトリ構成の修正 |

## 費用

| 項目 | 金額 |
|---|---|
| AI利用料金 | 正確な計測不可（既存プランの範囲内） |
| API料金 | 0円（api-sports.ioの無料アカウントを作成し無料プランで検証。有料プラン（PRO Plan 月$15〜）への加入は見送り、新規課金なし） |
| その他 | 0円 |

## 実施作業

- 「野球観戦した試合を記録するベイ日記（横浜DeNAベイスターズ）を作りたい」というリクエストから開始
- 試合日・対戦相手・スコア・勝敗・球場に加え、同行者・チケット代・天気/気温・写真・自分なりのMVP/活躍選手・興奮したシーンを記録できるフォームを実装
- 年度別の観戦成績（試合数・勝敗・分・勝率）の自動集計と、年度切り替え一覧画面を実装
- 「選手分析」画面：MVP=2pt・その他の活躍=1ptで集計し、よく名前が挙がった選手をランキング表示（クリックで対象試合・メモを展開）
- 写真はアップロード時にcanvasで縮小・JPEG圧縮してから IndexedDB に保存（localStorageの容量制限を避けるため）
- データの書き出し・読み込み（JSONエクスポート/インポート、写真込み）を設定画面に実装
- 試合結果の自動取得について、NPB公式データを安定・無料で返すAPIの有無を調査したが見当たらず、`api-sports.io`の任意キーを使う試験的機能として実装し、失敗時は常に手入力にフォールバックする設計にした
- ロジックのユニットテスト（勝敗判定・年度集計・選手分析、20件）と、実ブラウザでの操作を確認するsmokeテスト（記録の追加・編集・削除・写真添付・選手分析・設定、27件）を作成し、全件通過を確認
- PR作成後、開発環境のリポジトリが古い状態のままだったことに気づき、`apps/day23-baydiary/`への移動、ルートの`README.md`・`index.html`・`docs/progress.md`の修正をやり直した（詳細は下記「エラー・つまずき」）

## エラー・つまずき

- 開発環境のローカルチェックアウトが、リポジトリが `apps/dayNN-アプリ名/` 構成へ整理される前の古いコミット（チンチロを単体アプリとして持っていた頃）のままになっていることに気づかず作業を開始してしまった。そのため新しいアプリをリポジトリ直下に `bay-diary.html` として配置し、その状態でPRを作成した
- PR作成後に`origin/main`を確認して初めて、実際にはDAY20まで進んだ「30日で30アプリ」構成（`apps/dayNN-アプリ名/`・`docs/logs/`・`docs/progress.md`のルール）になっていることが分かり、`main`とのマージコンフリクト（`README.md`の内容衝突、新規ファイルの配置場所の食い違い）が発生した
- NPBの試合結果を安定して無料で返すAPIが実在するか確認できず、npb.jpや主要な候補サイトへの直接アクセスも開発環境のネットワーク制限で検証できなかった。実際にAPIが使えるかどうかは、ユーザー自身がapi-sports.ioのキーを試すまで分からない

## 解決方法

- `git fetch`で`origin/main`の実際の状態を確認し、リポジトリ構成のズレに気づいた時点で作業を止めてユーザーに確認（DAY番号として企画に組み込むか、単発アプリとして切り離すか）
- `origin/main`をマージし、`bay-diary.html`と関連テストを`apps/day23-baydiary/`（`index.html`・`test/logic.test.js`・`test/ui.smoke.js`）へ移動。ルートの`README.md`はコンフリクトを解消して`main`側の構成を採用しつつ、DAY23の行とテストコマンドを追記。`index.html`のトップページにもカードを追加
- 移動後に`node test/logic.test.js`・`node test/ui.smoke.js`を再実行し、パスを直した上で全件通過することを確認
- API連携は「安定した無料APIの実在が確認できない」という事実をそのままREADMEと本ログに明記し、手入力へのフォールバックを必須の設計にすることでリスクを下げた

## 追記：API-BASEBALLの実キーで検証（マージ後）

ユーザーがapi-sports.io（API-BASEBALL）の無料キーを実際に取得し、ベイ日記から「試合結果をAPIから取得」を試したところ `Cannot read properties of undefined (reading 'id')` で失敗した。原因は、`/leagues`のレスポンス形式をAPI-FOOTBALLと同じ`{league:{id,name}}`という入れ子構造だと推測して実装していたが、API-BASEBALLは`{id,name}`という直接構造の可能性が高く、存在しない`.league`への`.id`アクセスで例外になっていたこと。ドキュメント確認は開発環境のネットワーク制限で今回もできなかったため、`l.league || l`という形で両方の形式を許容する防御的な実装に直し、`/teams`・`/games`のレスポンス解析も同様に未確認箇所をnullチェックで固めた。

修正後、ユーザーが再度試すと今度は「チーム情報が見つかりませんでした」に変化。ユーザーにapi-sports.ioダッシュボードの「Live Demo」で`/leagues?country=Japan`を実際に叩いてもらい、レスポンス実物を確認したところ、以下が判明した。

- `/leagues`のレスポンスは推測どおりフラットな`{id,name,seasons:[...]}`構造だった（NPBは`id:2`）
- **`seasons`配列は新しい順に並んでいない**（`[2020,2026,2025,2019,...,2008,2009]`という順）。実装は`seasons[seasons.length-1]`（配列の最後）を「最新シーズン」とみなしていたため、実際には一番古い`2009`年を使ってチーム検索をしてしまい、2009年時点の名称（DeNA買収前の「横浜ベイスターズ」時代）と一致せず見つからなかったと推測される

`seasons`の中から`current:true`の要素（今回は2026年）を優先して使い、無ければ`season`の最大値を使うよう修正した。実際のレスポンスを使ったシミュレーションで`season=2026`が選ばれることを確認した。

修正後、ユーザーが再度試すと**再び「チーム情報が見つかりませんでした」**。ユーザーにLive Demoで`GET /teams?league=2&season=2026`（searchなし）を直接試してもらったところ、以下が判明した。

```
"errors": { "plan": "Free plans do not have access to this season, try from 2022 to 2024." }
"response": []
```

これはコードの不具合ではなく、**api-sports.io（API-BASEBALL）の無料プランの制限**そのものだった。無料プランは2022〜2024年シーズンのデータにしかアクセスできず、2025年・2026年（今シーズン）のデータは一切取得できない。つまり「観に行った今シーズンの試合を自動取得する」というベイ日記の主目的には、この無料APIは使えないという結論になった。

対応として、チームIDの特定だけは許可されている2024年シーズンにフォールバックして取得し（チームIDは年をまたいでも同じため一度特定できれば以降は使い回せる）、実際の試合結果取得が対象シーズン制限に引っかかった場合は、汎用的な「見つかりませんでした」ではなく`errors`に入っているAPI側のメッセージをそのまま表示するようにした。設定画面とREADMEにも、2022〜2024年シーズンのみ対応という制約を正直に明記した。この機能は今シーズンの用途では実質使えないが、後から過去シーズン（2022〜2024年）の試合を記録する場合には引き続き使える……はずだった。

ところが、ユーザーが実際に2024年の試合（2024/05/06 vs ヤクルト）で試すと、今度は`/games`側で別のエラーが返ってきた。

```
Free plans do not have access to this date, try from 2026-09-15 to 2026-09-17.
```

つまり`/teams`は「2022〜2024年シーズン」という粒度の制限だったが、`/games`（実際の試合結果）は**実行日（今日）を挟んだごく数日間の日付しか許可しない**、さらに厳しい制限だった。これはコードの不具合ではなく、修正済みのエラーメッセージ表示（`apiPlanError`）がAPI側の実際の制約をそのまま正しく見せてくれたことで判明した。

この時点で、「後から過去の観戦記録をつける」というベイ日記の使い方には、無料プランの`/games`はほぼ全く対応していないと結論づけた。これ以上コード側で回避する方法はなく（プランの契約上の制約のため）、設定画面とREADMEの説明を「基本は手入力、APIキーは任意でほぼ効果がない」というトーンに修正するにとどめた。

有料プラン（PRO Plan 月$15〜、ULTRA Plan 月$25、MEGA Plan 月$35）であれば日付・シーズンの制限が緩和される可能性があるが、ユーザーは個人の観戦日記アプリに月額$15以上を払う価値はないと判断し、**有料プランへの加入は見送り**。api-sports.ioのアカウントは無料のまま維持し、ベイ日記は手入力を基本運用とすることで決着した。新規の課金・購入は発生していない。

## 今日できるようになったこと

- 作業開始前に`origin/main`の実際の状態（コミットログ・ディレクトリ構成）を確認し、ローカルの前提が古くないかを検証する
- 個人の観戦記録のような「公式APIが整備されていない領域」では、外部API連携を主機能ではなく手入力を主・API取得を試験的な補助にする設計にする
- 写真をアプリ内で扱う際、localStorageではなくIndexedDBを使い、アップロード時にcanvasで縮小・圧縮してから保存する
- 「MVP選手」「その他の活躍選手」のような自由記述のタグ付けから、選手ごとの言及回数・重み付けスコアを集計してランキング化する

## 今日の動画で使えるポイント

1. PRを出した後になって、実は開発環境のリポジトリが古い状態のまま止まっていた（実際はDAY20まで進んだ「30日で30アプリ」構成）と判明し、配置し直した一部始終
2. 「NPBの試合結果を無料で安定して取れるAPIはあるか」を実際に調べ、npb.jp・Yahoo!スポーツ・ESPN・API-BASEBALL・BotchAPIなどを検証した結果、決定打がなく「試験的機能＋手入力フォールバック」という設計判断に落ち着いた過程
3. 写真・同行者・チケット代・天気まで含めた個人的な観戦日記から、「よく活躍を書き留めた選手」を自動でランキングする選手分析機能

## 追記：見た目の作り直し（マージ後）

PRマージ後、「デザインダサい」との評価を受けた。DAY24〜25（ポイまも）で確立した進め方（Claude Designで複数のビジュアル案を作りユーザーに選んでもらう）を踏襲し、3方向の案を作成：

- A. スタジアム/チケット風（本物の入場券のような質感、ミシン目・スタンプ・活版風タイポグラフィ）
- B. モダンスポーツアプリ風（ESPN風のダーク×ゴールド、絵文字を排除しアイコン化）
- C. エディトリアル/雑誌風（野球雑誌のような写真主体のレイアウト）

ユーザーはA案（チケット風）を選択。実装は`apps/day23-baydiary/index.html`のCSSと静的マークアップのみを対象にし、JSのロジック・要素ID・クラス名は一切変更していない（テストの前提を壊さないため）。具体的には：色を紺インク×クリーム紙×レンガ色のアクセントに変更、書体をShippori Mincho（見出し）＋Space Mono（数字・日付）＋Zen Kaku Gothic New（本文）に変更、試合カードに入場券のミシン目風の丸い切り欠きをCSSの疑似要素で追加、勝敗バッジを回転させたスタンプ風に変更、天気・同行者・チケット代の表示から絵文字（☀️👥🎫など）を排除。

書き換え後も`node test/logic.test.js`（20件）・`node test/ui.smoke.js`（27件）が全て通過することを確認し、Playwrightで一覧・選手分析・フォーム・設定の4画面をスクリーンショットで目視確認した。

## 追記：iOSアプリ化（マージ後）

ユーザーから「iOSでアプリ化しよう」というリクエスト。DAY26で確立した「旅の足跡」のiOS化（Capacitor＋GitHub ActionsのmacOSランナーでビルドしTestFlightへアップロード）とまったく同じ仕組みを、ベイ日記にも適用した。

- `apps/day23-baydiary-ios/`を新規作成。`capacitor.config.json`（`appId: com.hiroyaapps.baydiary`）・`package.json`は`day07-tabilog-ios`と同じ依存構成
- アプリアイコン（1024×1024）とスプラッシュ（2732×2732）は、ベイ日記のチケット風デザイン（紺インク・クリーム紙・レンガ色）に合わせた野球ボールのモチーフをSVGで描き、Playwrightでスクリーンショットして生成した（外部の画像生成ツールを使わず、既存のPlaywright環境で完結させた）
- `apps/day23-baydiary/privacy.html`を新規作成。本アプリはサーバーを持たずlocalStorage/IndexedDBのみに保存するため、「データは収集されません」と申告できる、旅の足跡よりシンプルな内容にした
- `.github/workflows/baydiary-ios-build.yml`は、`tabilog-ios-build.yml`をほぼそのまま流用。**Apple Developer Programの登録・Team ID・App Store Connect APIキー・配布用証明書は、旅の足跡（DAY26）で作った同じApple Developerアカウントのものをそのまま使い回せる**ため、追加費用は発生しない（年会費$99の重複登録は不要）。一方、**Bundle ID固有のプロビジョニングプロファイルだけはアプリごとに別物が必要**なため、新しいシークレット`IOS_BAYDIARY_PROVISIONING_PROFILE_BASE64`として区別した
- 写真添付でカメラ利用の説明文（`NSCameraUsageDescription`）が無いとクラッシュする、というDAY26で踏んだつまずきを、あらかじめInfo.plistへの追加ステップとして反映済み（同じ`<input type="file" accept="image/*">`を使っているため同じ問題が起きるはず）
- `apps/day23-baydiary-ios/app-store-listing.md`・`README.md`（Apple管理画面での作業手順、旅の足跡との使い回し表つき）も用意した

**未検証・ユーザー側の作業が必要な点**：このワークフロー自体はまだ一度もビルドを実行していない（tabilog-ios-build.ymlも実行検証済みとは限らない）。また、ベイ日記専用のBundle ID登録・App Store Connect上のアプリ作成・プロビジョニングプロファイル作成は、Appleの管理画面でユーザー自身が行う必要があり、まだ未実施。

## 追記：アプリ名を「ベイ日記」から「観戦日記」へリネーム

ユーザーから「ベイ日記という名前を、12球団の観戦記録なので観戦日記とかなんかいい感じの名前にして」というリクエスト。多球団対応の機能自体（`baydiary-handoff.md`に引き継ぎ済み）はまだ未着手だが、いずれ12球団のファンが使える前提のアプリだと分かるよう、まず名前だけを先に直した。

- `apps/day23-baydiary/index.html`：タイトルタグ・見出しのロゴ文字・設定画面の削除確認文言を「観戦日記」に変更。ヘッダー上部の小見出しは`YOKOHAMA DeNA BAYSTARS`という球団名決め打ちの表記だったのを`NPB ADMISSION LOG`という球団非依存の表記に変更
- `apps/day23-baydiary/README.md`・`privacy.html`：「観戦日記（旧名『ベイ日記』）」であることと、**機能面は今も横浜DeNAベイスターズ専用のまま**（対戦相手の選択肢やAPI検索条件がベイスターズ視点でハードコードされている）ことを明記
- ルートの`index.html`（アプリ一覧ページ）のDAY23カードも名前・説明文を更新
- `apps/day23-baydiary-ios/`（Capacitor設定・README・app-store-listing.md・ワークフローファイルの表示名）も同様に「観戦日記」へ統一
- アプリアイコン・スプラッシュ画面を作り直した。旧アイコンは紺背景に野球ボールだけのシンプルな絵だったが、ユーザーから「それっぽいアイコンも作ってくれる？」と依頼があり、アプリの「チケット風」デザインをより表現するため、**チケットの半券（左右に切り欠き・中央にミシン目風の点線・野球ボールの縫い目・金色の入場スタンプ風の星バッジ）**というモチーフに描き直した。スプラッシュ画面も同じモチーフ＋「観戦日記」の題字＋球団非依存の「NPB ADMISSION LOG」という副題に統一。どちらも旧アイコンと同じく、外部の画像生成ツールを使わずSVG＋Playwrightのスクリーンショットだけで作成した
- `docs/progress.md`・`docs/baydiary-handoff.md`も「観戦日記（旧ベイ日記）」という表記に更新（過去の経緯を追えるよう、旧名への言及自体は残した）

リネームは名前・見た目だけの変更で、`index.html`のロジック部分（`LOGIC_MARK_START`〜`LOGIC_MARK_END`）やHTML要素のid・class名は変更していないため、`node test/logic.test.js`（20件）・`node test/ui.smoke.js`（27件）は変更前と同じ内容で全て通過することを確認した。

## DAY RESULT

```
DAY 23 RESULT
アプリ：ベイ日記（横浜DeNAベイスターズの観戦記録日記）
人間：1h30m（本人申告・概算）
AI：2h0m（本人申告・概算）
費用：0円（api-sports.io有料プランへの加入は見送り、新規課金なし。iOS化もApple Developerアカウントを旅の足跡と共有するため追加費用なし）
今日できるようになったこと
・観戦記録のような公式API未整備の領域で、手入力を主・API取得を試験的補助にする設計判断
・写真をIndexedDBに縮小保存し、localStorageの容量制限を避ける
・「デザインダサい」との指摘から、Claude Designで複数案を作りユーザーに選んでもらう進め方
・外部APIのエラーメッセージをそのまま表示する実装にしたことで、コードの推測ミスとAPI側の契約上の制約を素早く切り分けられた
・別アプリ（旅の足跡）で確立したiOS化の仕組みを、Bundle ID固有の部分だけ差し替えて別アプリに横展開する
・アプリアイコン・スプラッシュをPlaywrightでのスクリーンショット生成だけで用意する
つまずいたこと
・開発環境のリポジトリが古い状態のまま止まっていることに気づかず、PR作成後に配置をやり直した
・NPBの試合結果を安定・無料で返す公式API相当のものが見つからなかった
・API-BASEBALLの無料プランは、チーム情報が2022〜2024年シーズン限定、試合結果はさらに厳しく実行日前後のみという制約があり、今シーズンの観戦記録には実質使えなかった
・複数セッションが同じ作業ブランチ名を使い回す運用のため、未マージのPRを見失いかけるミスを2回起こした（その都度origin側から復旧）
あと7 apps
```


## 追記：12球団・成績分析・デザイン刷新（2026-09-24）

最新の依頼に合わせ、従来のチケット風から紺・白・ブルーを基調としたスポーツダッシュボードへ変更。ホームに今季・通算・直近6試合、独立した成績分析に年度別・月別・球場別とチーム成績比較を追加した。応援球団は12球団から選択でき、試合ごとに球団を保存する。既存データはDeNAとして扱い、写真DB・保存キーは維持した。

比較成績は球団×年度単位の手入力。外部APIの無料制約を再調査せず、任意の試合取得機能だけ選択球団に対応させた。詳細分析はPlusプレビューとして無料公開し、料金・決済・有料アクセス制限は未実装。サーバー側の利用権検証を将来の導入条件としてREADMEに記録した。

検証：ロジック43件、既存UI27件、新規分析UI46件が通過。320・390・1280px、旧データ移行、球団と年度の分離、チーム比較、写真を含む書き出し・読み込み、APIのアウェイ判定（モック）、文字列のHTMLエスケープを確認。実APIへのアクセスや課金は行っていない。デスクトップとスマートフォンのホーム・分析画面は画像でも確認した。

元の未マージブランチから専用作業ブランチ codex/baydiary-stats-redesign を作成。元の作業フォルダ・未追跡ファイルは変更していない。時間・モデル別トークン費用は計測していないため追記しない。

## 追記：結果共有とAIメモ一括登録（2026-09-24）

成績を1080×1350のPNGと投稿文にする共有画面を追加。年度・通算・直近6試合・球場別を選択でき、アプリ名とハッシュタグを掲載。Web Shareのファイル共有と画像ダウンロード・テキストコピーを用意した。公開アプリURLは設定式とし、未公開ストアへの架空リンクは作らない。

過去メモの貼付・複数テキストファイルからOpenAIでJSON化し、行ごとの検証・重複候補の除外・ユーザー確認後に追加する。既存のデータ・設定・写真を維持し、容量不足時は変更をコミットしない。原文を保存し、JSONの修正・書出・再取込にも対応。AIキーはサーバーの環境変数に限定。ローカル開発用サーバーを追加した。

検証は従来116件＋ロジック/サーバー15件＋新UI35件で166件。共有画像のダウンロード、共有呼出（スタブ）、重複・日付不明・得点矛盾・保存失敗・AI接続失敗を確認。320/390/1280pxで横はみ出しなし。実OpenAIキーは未設定のためライブ抽出は未検証、iOS実機の共有も未検証。API課金の実行なし。

## 追記：交流戦・CS・日本シリーズの別集計（2026-09-24）

4種類の大会区分を試合に保存し、ホームに交流戦・CS・日本シリーズを独立表示。公式戦は通常の公式戦＋交流戦、ポストシーズンは別集計とする。大会別一覧、試合一覧・成績分析のフィルタ、共有画像の大会表示、AI抽出とJSON取込にも反映した。古い記録は通常の公式戦として移行し、編集で変更できる。

通常のシーズン勝率との比較は公式戦集計のみで表示する。CSと日本シリーズは独立して勝敗・勝率・年度別/月別/球場別成績を確認できる。既存166件＋大会ロジック6件＋ブラウザ26件で198件通過。スマートフォン表示を画像でも確認した。


## 追記：使えていない試合結果APIを非表示（2026-09-24）

試合結果APIの取得ボタンと設定カードを非表示にした。手入力・AIメモ取込は継続。写真読込失敗も表示するステータス欄は残し、既存設定の削除は行わない。既存ブラウザテストの表示前提を更新した。

## 追記：スマホ表示の細かい不具合修正とAI一括登録の公開準備（2026-09-25）

このタイミングで、Codex（別のAIコーディングツール）がこのアプリを並行して大幅に作り込んでいたことが判明した（デザイン刷新・交流戦/CS/日本シリーズ集計・AIメモ取込・iOSのSwiftネイティブタブなど）。ユーザーから「そっちでもやろうとしていたけどこっちでやりましょう」と指示があり、Claude側のセッションで作業を継続することにした。同じアプリに複数のAIツールが並行して手を入れる状態になっていたため、今後どちらか一方に寄せるか、変更のたびにすり合わせる必要がある。

実機（iPhone）のスクリーンショットをもとに、次の不具合を修正した。

- ピンチズームすると戻せなくなる：`<meta name="viewport">`に`maximum-scale=1, user-scalable=no`を追加し、そもそも拡大できないようにした
- ホーム画面下部の成績カード（通算成績・直近6試合）が、iOSネイティブの下タブバーに隠れて潰れて見える：`.bay-native-shell .wrap`に下側の余白を追加
- 記録フォームの「同行者」「写真」「その他 活躍した選手」の見出し（`<legend>`）が、背景の丸い箱に食い込んで重なって見える：`legend`に`float:left;width:100%;margin-bottom:8px`を指定し、`<fieldset>`の縁からはみ出さないようにした（`<legend>`をCSSで独立させる定番の対処）

いずれも見た目だけの修正で、ロジック部分・要素ID・クラス名は変更していない。修正後、既存テスト（logic 43件・ui.smoke 27件・features 17件・competition 12件・impressions/quality/nativeはこの環境にMicrosoft Edgeが無く実行不可）がすべて通過することを確認した。

また、「メモから一括登録」のAI読み取り機能について、ユーザーから「お金がかかってもいいので実際に動くようにしてほしい。1回200試合まで、端末ごとに3回まで」という要望があった。調べたところ、この機能はCodexによってすでにコードとしては作り込まれていたが、（1）読み取り先の`/api/memo-extract`を提供するサーバーが存在せず、GitHub Pages上のWeb版では404になる、（2）iOS版では明示的に「準備中」として無効化されている、という状態で、実際には一度も動いていなかった。

ユーザーに「どこにホスティングするか」「AI読み取りにOpenAI/Anthropicどちらを使うか」「回数制限の具体的な仕様」を確認した上で、以下を実装した。

- `apps/day23-baydiary-api/`を新規追加。Vercelのサーバーレス関数として`/api/memo-extract`を公開する。ロジックの二重管理を避けるため、変換ルール（`TEAMS`・`MEMO_SCHEMA`・`memoInstructions()`）は複製せず、`apps/day23-baydiary/index.html`から実行時に読み込む方式にした（`server.cjs`と同じ手法）。デプロイ設定の詳細は`apps/day23-baydiary-api/README.md`にまとめた（OpenAI APIキーの取得、Vercelプロジェクト作成、Vercel KVでの利用回数記録）
- 1回あたりの上限を100試合→200試合に変更（`server.cjs`・クライアントの指示文の両方）
- 端末ごとに生涯3回までの利用制限を追加。`X-Device-Id`ヘッダー（`localStorage`に保存した乱数ID）で識別し、超過時は「この端末での利用上限（3回）を超えました。」を返す。ローカル開発用の`server.cjs`はメモリ上のMapで、公開用のVercel関数はVercel KVで、それぞれ回数を記録する
- Web版（`index.html`）はAPIの接続先を`<meta name="baydiary-api-base">`から読むようにした（デプロイ先が決まるまでは空欄で、`server.cjs`経由のローカル動作に影響しない）
- iOS版（`native.mjs`）の「準備中」エラーを削除し、実際に公開APIへ接続するようにした（接続先はコード内の`API_BASE`定数）

**AI接続はまだ実際には検証していない**（OpenAI APIキーの取得・Vercelへのデプロイはユーザー側の作業が必要なため、未実施）。新規追加分は`apps/day23-baydiary-api/test/memo-extract.test.cjs`（8件、OpenAI・Vercel KVへの実接続なしのモックテスト）と、`server.cjs`側の追加テスト（3件）で検証し、既存分と合わせて全件通過を確認した。
