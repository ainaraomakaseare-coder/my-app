# DAY7：旅の足跡

**新しい挑戦**：初めてのデータベース（保存が端末をまたいで残り、複数人で共有できる）

**番号について**：ロードマップ上どのDAYにするかはユーザーと保留にしたまま実装を優先している（DAY7は「初めてのデータベース」という学びの内容が今回の題材と合うため仮に割り当て）。`docs/progress.md` の表はまだ更新していない。

## 時間

| | |
|---|---|
| 開発開始 | 2026-09-12 07:25 |
| 開発終了 | 2026-09-12 07:35（区切りとして。実際にはこの後もユーザーとのやり取りが続く想定） |
| 追加セッション開始 | 2026-09-12（同日内。外部Skill導入＋評価/マイログ機能のヒアリングと実装） |
| 追加セッション開始 | 2026-09-13（見た目のSlack/Confluence風への作り直し、日ごとの天気機能、Googleログイン設定とiOSアプリ内WebViewの制約発見、メールログインのOTP化、アプリ名を「旅の足跡」に変更） |
| 人間作業 | 正確な計測不可 |
| AI実稼働 | 正確な計測不可 |
| AI待機 | 正確な計測不可 |

## 使用モデル

| モデル | 作業内容 |
|---|---|
| Claude（Claude Code） | Claude Designでの見た目のデザイン案作成、要件のヒアリング、アプリ本体（フロント・Worker）の実装 |

## 費用

| 項目 | 金額 |
|---|---|
| AI利用料金 | 0円（このアプリ自体はAI呼び出しを持たない。DAY18のようなAI深掘りは今回スコープ外） |
| API料金 | 0円想定（Cloudflare Workers・D1・R2はいずれも無料枠内で運用する方針） |
| その他 | |

## 実施作業

- ユーザーからの「旅の思い出を写真とともに振り返るアプリ」というリクエストを、Claude Designでビジュアルのモックアップ（旅の足跡）として先に作成。対話の中で要望が広がった（費用・評価・地図リンク・予約サイト・メンバー招待・別行動タグ・音声での旅程復元・DB化）ため、`interview-me` の考え方で優先度を確認する質問をはさんだ
- 確定した内容：見た目の参考はPolarsteps、音声での旅程復元機能は今回は見送り、データベース化は無料枠に収める方針
- `apps/day07-tabilog/` を新規作成。フロント（index.html・style.css・app.js）とバックエンド（Cloudflare Worker + D1 + R2、`worker/`）を分離して実装
  - Trip（旅行）・Episode（記録）の2つのテーブルで、旅行の日程・参加者・記録ごとの時間・場所・写真・費用・楽しかった度（★）・種類（観光／食事／宿泊／移動／その他）・地図URL・公式サイトURL・予約サイト名・別行動タグを持たせた
  - ログイン機能は作らず、旅行ごとに発行するIDを含むURLを知っている人だけが読み書きできる「リンク共有」方式にした
  - 写真はブラウザ側で縮小・圧縮してからWorker経由でR2にアップロードする構成
- `docs/logs/TEMPLATE.md` を元にこのログを作成。ロードマップ上のDAY番号はユーザーと保留中のため、学びの内容が合うDAY7を仮に割り当てた（`docs/progress.md` の表は未更新）
- ユーザーのCloudflareアカウントでD1・R2を作成し、Workerを実際に公開（`https://tabilog-api.hiroya-apps.workers.dev`）。ユーザーのWindows PC上でwrangler CLIを操作してもらう形で進めた
- 公開後、ユーザーから見た目（るるぶ風の演出をやめてSlackのようなモダンな見た目に）と、データの持ち方（記録を「大項目（予定）」「小項目（一人ひとりの記録）」の2階層に分け、別行動は同じ大項目に小項目を複数ぶら下げて表現する）について大きな修正依頼があり、フロント・Worker・スキーマを作り直した
- 動画添付（1本50MBまで）・詳細欄・Google/Appleログイン（任意設定、記録した人への自動入力用）・App Store公開に向けたiOSアプリ化（Capacitor + GitHub Actions）・プライバシーポリシー・申請下書きを追加
- `.claude/skills/` に、mattpocock/skills・addyosmani/agent-skillsから7つのSkill（codebase-design・grill-with-docs・to-spec・to-tickets・wayfinder・documentation-and-adrs・idea-refine・planning-and-task-breakdown）をMITライセンスのまま導入（プロジェクトスコープのみ。グローバルには追加していない）
- `interview-me` の型（仮説→確信度→一問ずつ質問→確定）で「要件定義の強化・デザインの見直し」をヒアリング。「各記録に評価と費用をつけたい」という最初期の要望のうち費用だけ実装され評価が漏れていたことを発見し、単純な★1個ではなく、カテゴリ別（飯ログ／ほてログ／アクティビティーログ／移動ログ／その他ログ）に整理し、複数の旅行を横断して振り返れる「マイログ」画面として要件を確定させた
- 確定した要件を実装：
  - Worker：D1に`ratings`テーブルを追加（entry×ログインした人ごとに1件、UNIQUE制約でupsert。既存テーブルは変更しないため過去データは消えない）。`PUT/DELETE /entries/:id/rating`・`GET /mylog?email=`を追加。`GET /trips/:id`のレスポンスにentryごとの評価一覧を含めた。旅行・予定・記録の削除時に紐づく評価も一緒に消すようにした
  - フロント：ログインを「アプリ全体の入り口をブロックする」仕組みから「評価・マイログを使うときだけ求める」仕組みに設計変更（閲覧・記録の追加は今までどおりログイン不要のまま）。記録の編集画面に★1〜5の評価ウィジェット（自分の評価はワンタップで即保存、みんなの平均も表示）、ホーム画面に新しい「マイログ」画面（カテゴリタブ×評価順/新しい順の切り替え）を追加
  - `CONTEXT.md`に評価・マイログ・ログインの位置づけ（本人確認であってアクセス制御ではない）を追記
  - Playwrightで実際の画面をスクリーンショット確認（ログイン前後・評価入力・マイログ一覧）してからテストに反映
- 見た目の再修正依頼「Slackとかコンフルエンスを教科書とした色味や図形を使ったモダーンな感じに」を受け、CSSのトークン（背景・アクセント色・カード角丸12px・薄いぼかし影・カテゴリを色付きピルで表示）を全面的に作り直した
- 「天気とかその場所とか日時をもとに天気と気温を入れたい」という要望を`interview-me`で深掘りし、Block（予定）単位ではなくTrip×日付単位（DayInfo）で持つ設計に確定。Open-Meteo（無料・APIキー不要）でジオコーディング→天気取得をWorker側で自動実行し、過去日は実況(archive-api)・当日以降は予報(forecast api)を使い分け、一度取得した値はDBにキャッシュする仕組みを実装
  - ユーザーから「甲府で雨と出たが実際は降っていなかった」という報告を受け、Open-Meteoの過去実況が観測所データではなく再解析（モデル推定）であることが原因と考えられたため、降水量(precip_sum)を追加し「1mm以下なら雨系コードでも表示は曇りにする」という判定を追加
- 「旅行が消せない」という要望を確認したところ、実際の動機は「サンプルデータを一括で消したい」だったため、削除UIではなく`reset-sample-data.sql`（`trips`・`blocks`・`entries`・`ratings`・`day_infos`を全件削除するスクリプト）を用意。将来もし削除UIを作るなら、うっかり削除を防ぐため確認ダイアログだけでなく確認コードを求めたい、というユーザーの希望を`CONTEXT.md`に記録した
- 「Googleでのログイン機能作ろうよ」を受けてGoogle Cloud Console側の設定を進める中で、ユーザーから「これって今後iOSアプリでできるようにする前提はわかっていますか？」と問われ、GoogleがOAuthログインをアプリ内蔵WebViewから行うことをセキュリティ上ブロックしていることを確認。CapacitorでWeb版を包んだiOSアプリの中では、今のGoogleログイン（Web向けJSライブラリ）が動かない可能性が高いという設計上の見落としが判明した
  - この発見を受け、ユーザーの提案「メールアドレス認証でメールが送られてワンタイムパスワードをいれる形」に沿って、それまでの「その場で入力するだけで検証しない」簡易メールログインを、実際にメールでコードを送って照合する本物のOTP認証に置き換えた（`email_otps`テーブル、`POST /auth/email/send`・`POST /auth/email/verify`、送信元はResendを採用）
- ユーザーの指示「旅ログって名前ではなく、旅の足跡ってアプリ名になっている」を受け、ユーザー向けの表示名を「たびログ」から「旅の足跡」へリポジトリ全体で変更（フォルダ名・技術的な識別子（JSの`TabiLog`名前空間・APIドメイン・Capacitorの`appId`）は既存リンクが壊れないよう、あえて変更していない）
- 「参加者（companions）をアカウントと紐付けたい」という要望を`interview-me`で深掘り。最初の説明だけでは「マイログに何が変わるのか」が曖昧だったため、一問ずつ確認したところ「ホーム画面の旅行一覧がlocalStorage頼りで他の端末から見えない」という前から分かっていた弱さを解消したいのが本当の動機だと判明。「マイログに、評価の細かいログとは別の“参加した旅行一覧”を追加する」という要件に確定させた
- `domain-modeling`で決めた用語（アカウント／アカウントID／参加者＝ゲスト参加者とアカウント参加者の2種類）を`CONTEXT.md`に追記し、「メールアドレスではなく6桁のアカウントIDで参加者を紐付ける」という設計判断を`docs/adr/0001-account-id-for-participants.md`に記録してから実装に入った
- 確定した要件を実装：
  - Worker：`accounts`（メールアドレス→6桁のアカウントID）・`trip_members`（Trip×アカウントの紐付け）の2テーブルを追加。`POST /accounts/ensure`（ログイン成功のたびに呼び、アカウントを作成/取得）・`POST /trips/:id/join`（「参加する」）を新設し、`GET /trips/:id`のレスポンスに参加者一覧を、`GET /mylog`のレスポンスに参加した旅行一覧を追加した
  - フロント：旅行詳細画面に「参加する」ボタン、マイログ画面に「参加した旅行一覧」を追加。Google/Apple/メールOTP、3つのログイン方法すべてで、ログイン成功後にアカウントIDを取得する処理を共通化した
  - Playwrightのテストに、未ログインで「参加する」を押すとログイン画面に行くこと・参加後に参加済み表示になること・マイログに参加した旅行が出ることを追加

## エラー・つまずき

- テスト実行時、`playwright` はグローバルにインストールされていたが、このリポジトリのフォルダからは `require('playwright')` が解決できず `MODULE_NOT_FOUND` になった（ローカルの `node_modules` が無く、Node標準のモジュール解決では `/opt/node22/lib/node_modules` のようなグローバル置き場を辿らないため）
- この作業環境（クラウドのサンドボックス）は組織のネットワークポリシーで `api.cloudflare.com` への接続がブロックされており、Cloudflareへのデプロイ作業を代行できなかった。ユーザー自身のPCで `wrangler` コマンドを実行してもらう形に切り替えた
- ユーザーのPCがWindows PowerShellで、`npm install` がスクリプト実行ポリシーによりブロックされた（`UnauthorizedAccess`）。また、R2の利用開始時にCloudflare側で支払い方法の登録（サブスクリプション契約。無料枠内なら0円）が必須だった
- 見た目を確認する手段が、実際に動くアプリ（ローカルサーバーの起動に手間取っていた）ではなく、先に作ったClaude Designのモックアップしか無かったため、コード側だけ直してもユーザーには変化が伝わらなかった
- 評価機能を設計する中で、「マイログ」で複数の旅行を横断して評価を見るには、ログインしたアカウント（メールアドレス）で誰の評価かをサーバー側に紐づける必要が新たに出てきた。一方でこのAPI・アプリ全体はサーバー側でGoogle/Appleのトークンを検証しない簡易的な仕組みのため、「メールアドレスさえ分かれば誰でもその人のマイログを覗けてしまう」という新しいプライバシー上の弱点が生まれた。本格的なトークン検証（JWKS・署名確認）まで実装するかどうかを検討したが、家族向けの低リスクな前提・既存の設計方針との一貫性・当日中に検証しきれないUX（トークン失効時の挙動）を考慮し、今回は既存の簡易的な仕組みのまま実装し、この弱点をREADME/CONTEXT.mdに明記する判断にした
- 作業環境（クラウドのサンドボックス）からは`api.open-meteo.com`・`tabilog-api.hiroya-apps.workers.dev`・実際の公開URLのいずれにも接続できず、天気機能・OTPログインとも実機での動作確認をユーザー自身にお願いする形になった（コード側はユニットテスト・Playwrightのモックで検証）
- Googleログインの設定を進める中で「iOSアプリでも使える前提か」と聞かれるまで、GoogleがOAuthをアプリ内蔵WebViewからブロックしている点に気づいていなかった。事前の設計段階でこの制約を洗い出せておらず、ユーザーからの質問で初めて発覚した
- ユーザーのPCでのApple Developer Program登録が「Membership」の設定待ちで止まったり、App Store Connect用の設定が反映されるまで時間がかかったりと、Apple側の審査・反映待ちによる中断が複数回発生した

## 解決方法

- `NODE_PATH=/opt/node22/lib/node_modules` を付けて実行することで解決した。恒久的にはこのアプリ用に `npm install playwright` するのが望ましいが、今回は環境側の制約としてこの回避策で確認した
- デプロイはユーザーのPC側で実行してもらい、こちらは手順を都度案内する形にした。PowerShellの制限は `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` で解消
- 見た目の修正依頼が来た際、コードだけでなくClaude Designのモックアップ側も同じ内容に直し、確認手段を用意した
- 作業環境から外部APIに繋がらない前提のまま、ユニットテスト・Playwrightのモックで作り込み、実機確認はユーザーに依頼して結果を都度共有してもらう形に切り替えた
- Googleログインのアプリ内WebView制約が判明した際、Google側のSDKを新たに組み込むのではなく、ユーザー自身の提案（メールでのOTP認証）を採用し、埋め込みWebViewの制約を受けない仕組みに設計を切り替えた

## 今日できるようになったこと

- 保存先を端末内（localStorage）からサーバー（データベース）に変える設計判断と実装（Cloudflare D1・R2）
- ログイン機能を作らずに「リンクを知っていれば読み書きできる」共有の仕組みを設計する
- Claude Designで先にビジュアルのモックアップを作ってから、対話で要望を広げ、優先度を確認してから実装に入るという進め方
- ユーザー自身のPCでwranglerコマンドを実行してもらいながら、CloudflareへWorker・D1・R2を実際に公開する
- 公開後に来た「見た目」「データ構造」の大きな修正依頼に対し、モックアップとコードの両方を作り直す
- ネットワークポリシーでこちらの作業環境から外部サービスに繋げない場合、ユーザーの端末での作業に切り替える判断
- 「ログイン＝アプリ全体への関所」という最初の設計を、要件が変わったタイミングで「ログイン＝特定の機能（評価）だけの関所」に作り変える（既存のログイン関連のテストも、その前提の変化に合わせて書き直す）
- 新しい機能を追加するときに生まれる、機能単体では見えにくいセキュリティ上のトレードオフ（メールアドレスだけを鍵にする設計の弱点）に気づき、実装するかどうかをその場で判断してドキュメントに残す
- 外部からSkillを導入する際、「グローバルに入れるべきか・プロジェクトだけにすべきか」をユーザーと確認し、企画のルール（Skillは数を必要最低限に）と実際の運用を照らし合わせる
- Playwrightでの自動テストに加えて、実際の画面をスクリーンショットで目視確認してからテストコードに反映する（UIの見た目が意図通りかをコードだけで判断しない）
- 日ごとの天気のような「外部APIから取ってきてキャッシュする」機能の設計（取得タイミング・過去/未来の切り替え・再取得しない仕組み）
- 「雨と出たが実際は降っていなかった」という実際の利用報告から、外部データの性質（再解析データであること）を踏まえた閾値判定を追加する、という現場発の仕様調整
- 「削除したい」という要望の裏にある本当の動機（サンプルデータの一掃）を確認してから機能を決める、要件の掘り下げ
- Googleログインがアプリ内蔵WebViewでブロックされるという、実機・実プラットフォーム特有の制約を途中で発見し、設計を作り直す（メール認証をOTP方式に置き換える）
- アプリの表示名（ユーザー向けの見た目）と、フォルダ名・APIドメイン・パッケージID等の技術的な識別子を区別し、後者は互換性のためあえて変更しないという判断

## 今日の動画で使えるポイント

1. 「まず見た目のモックアップを見せる→そこに乗っかって要望がどんどん広がる」という、要件が対話の中で育っていく様子がそのまま撮れる
2. グローバルにインストール済みのはずのツール（playwright）が、フォルダの位置によっては見つからないという、初心者がつまずきやすい「Node.jsのモジュール解決」の実例
3. 「音声で旅程を復元する」「AIで旅程を整理する」など魅力的な追加要望が次々出てきても、その場で全部やらずに「今回はここまで」と線を引く判断も動画の見せどころになる
4. Cloudflareへの実際のデプロイを、Windows PowerShellでのつまずき（実行ポリシー、日本語表示）を含めてそのまま追体験できる
5. AIの作業環境自体がネットワークポリシーでブロックされ、「代わりにやります」が通用しない場面があるという、AIの限界が見える瞬間
6. ユーザーが寝る前に「大きな変更でも判断して進めていい、お金がかかることだけ相談して」と一言残すだけで、要件のヒアリング→設計判断→実装→テスト→ビジュアル確認→ドキュメント更新までを一晩で終えられる、という自律的な開発の実例
7. 「評価機能をどう作るか」を決める過程で、機能追加のはずが「今のログインの設計（アプリ全体をブロックする関所）を見直す」という、当初想定していなかった既存設計への手戻りが必要になった場面（要件は後から育つと、前の設計判断まで揺さぶられることがある）

## DAY RESULT

```
DAY7 RESULT
アプリ：旅の足跡
人間：正確な計測不可
AI：正確な計測不可
費用：0円（Cloudflare無料枠内で運用する構成。実際にWorker・D1・R2を公開して使い始めた後の実費用は別途確認が必要）
今日できるようになったこと
・保存先を端末内からサーバー（データベース）に変える設計と実装
・ログイン不要の「リンク共有」方式の設計
・Claude Designでのモックアップ→対話で要望を広げる→実装、という進め方
・実際にCloudflareへWorker・D1・R2を公開する
つまずいたこと
・グローバルインストール済みのplaywrightがフォルダ位置によって見つからない（NODE_PATHで解決）
・作業環境からCloudflareへの接続がネットワークポリシーでブロックされ、デプロイをユーザー側で実行してもらう形に変更
・WindowsのPowerShellでnpmがスクリプト実行ポリシーによりブロックされる
あと未定 apps（ロードマップの番号整理は保留中）
```
