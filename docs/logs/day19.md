# DAY 19：おもいでWiki（AI×音声インタビューのブラッシュアップ）

**新しい挑戦**：音声で話しかけると、AIが答えの内容に応じてその場で追加の質問を返してくる「深掘りインタビュー」を作る（DAY10のドラマ王と同じCloudflare Worker経由のOpenAI連携を、対話の深掘りに応用）

## 経緯

DAY18で作った「おもいでWiki」について、interview-me Skillの型（仮説→確信度→一問ずつ質問→確定）でユーザーとヒアリングを実施。確定した要件は `apps/day18-omoide-wiki/requirements-day19-20.md` を参照。

要点：
- 固定の質問リストだけでは「留学のとき何を思っていたか」のような話を引き出しきれないため、回答内容にAIが反応して追加質問を作る機能が必要
- 音声で答えられるほうが早く多く話せるので、この深掘りは音声でのやり取りを主役にする
- サークル旅行のエピソードは別アプリにはせず、同じ `apps/day18-omoide-wiki/` の中で「旅行」の区分として整理する
- 車内トークの録音・書き起こし（③）はDAY20へ持ち越し
- アプリは分割しない（データを1箇所にまとめておきたいため）

## 時間

| | |
|---|---|
| 開発開始 | 2026-09-11 07:38 (UTC) |
| 開発終了 | 2026-09-11 07:47 (UTC) |
| 人間作業 | 0h（自動実行セッション） |
| AI実稼働 | 正確な計測不可 |
| AI待機 | 0h |

## 使用モデル

| モデル | 作業内容 |
|---|---|
| Claude（Sonnet 5 系） | ヒアリング（interview-me Skill）・設計・実装・テスト |

## 費用

| 項目 | 金額 |
|---|---|
| AI利用料金 | 正確な計測不可（既存のClaude Codeプラン内） |
| API料金（OpenAI, Worker経由） | 今回は未使用（Workerのコードのみ実装。実際のデプロイ・APIキー登録・課金はユーザー側の作業） |
| その他 | 0円 |

## 実施作業

- `interview-me` Skillの型（仮説→確信度→一問ずつ質問→確定）でヒアリングを実施し、`apps/day18-omoide-wiki/requirements-day19-20.md` に確定内容を記録
- DAY05のドラマ王Workerを参考に、`apps/day18-omoide-wiki/worker/` を新規作成（CORS・レート制限・秘密鍵の扱いは同じ構成。個人の回答を扱うためキャッシュだけ外した）
- インタビュー画面に「AIで深掘りする」トグルを追加。回答後にWorkerへ問い合わせ、返ってきた追加質問を**既存のインタビューの質問キューにその場で差し込む**形で実装（新しい画面やボタンを作らず、既存の「音声で読み上げ→話して答える」ループをそのまま使い回せる設計にした）
- 1つの話題につき深掘りは最大3往復までクライアント側で強制的に打ち切る仕組みを追加（Workerが`done`を返さなくても止まる）
- エピソードに「旅行・イベント名」の任意項目を追加し、完成ページのアルバムをその名前ごとにグループ化する機能を追加（`groupEpisodesByTrip`としてテスト可能な形で実装）
- データ変換テストを5件、実ブラウザテストを8件追加（フェイクのWorkerを立てて、AI深掘りの一連の流れとクライアント側の上限を検証）
- README・トップページ・進捗表を更新

## エラー・つまずき

- 実ブラウザテストで、テスト用に立てたフェイクのAI応答サーバーへの`fetch`がCORSでブロックされ、質問がいつまで経っても切り替わらずタイムアウトした
- （本番のWorker自体はCORS対応済みだが、テストで使い捨てで立てた簡易サーバーにCORSヘッダーを付け忘れていたのが原因）

## 解決方法

- フェイクサーバー側に`Access-Control-Allow-Origin`と`OPTIONS`ハンドリングを追加し、実際のWorkerと同じ経路（プリフライト→POST）で検証できるようにした

## 今日できるようになったこと

- `interview-me` Skillを実際の要件ヒアリングに使い、行き当たりばったりに広がっていた要望（人生の深掘り・サークル旅行・車内録音）を1つずつ言語化してから実装に入る
- 個人的な回答をAIに読ませて、その場で自然な追加質問を1つだけ作らせる（キャッシュせず、深掘り回数に上限を設けて費用を抑える設計）
- 既存の「音声で読み上げ→話して答える」ループに、決め打ちでない動的な質問をそのまま乗せる設計（新しい仕組みを足すのではなく、質問キューに差し込む形で既存の画面遷移を使い回した）
- 「アプリを分けるか／1つにまとめるか」をユーザーと一緒に判断し、理由（データを1箇所にまとめておきたい）を言葉にしてから決める

## 追記23（過ごした一日＝大項目・小項目の記録／評価機能）

「Slackみたいなモダーンな仕上げにしてほしい。AI感が出ないような。あとモデルコースではなく、過ごしたコースなのでうまい言い方してほしい。記録を大項目と小項目にしてほしい」という要望。旅行の詳細画面に「過ごした一日」という新しいセクションを追加した。「モデルコース」（お手本の予定表）と混同されないよう、あえてその単語を避け、「モデルコースではなく、実際に過ごした記録です」という説明文を添えている。

構造は要望どおり2段になっている。

- **大項目（Stop）**：日付・時間・タイミング（〇〇に集合、××に到着など）・種類（移動／食事／観光など）
- **小項目（StopDetail）**：エピソード・写真・一言・一人あたりの値段（＋明細の追加）・待ち時間・地図のURL・店のHP・記録した人

「別行動した場合は小項目を分ければいい」との指定どおり、1つの大項目の下に複数の小項目をぶら下げられる設計にした。同じ「12:00 到着・食事」という大項目の中に、太郎の「ラーメンを食べた」という記録と、花子の「カフェでパフェを食べた」という記録を別々に残せる。

データの持ち方は、Trip（追記14）のときと同じ考え方で、`stops`・`stopDetails`をWikiの直下に持つ配列にし、`tripId`／`stopId`で親を参照する形にした（大項目の中に小項目の配列をネストする持ち方も考えたが、それだと複数人がそれぞれ書き足した内容をJSONで合体するときに、片方の更新がもう片方の追加をまるごと上書きしてしまう恐れがある。既存のマージの仕組みをそのまま安全に使えるよう、あえてフラットな持ち方にした）。

見た目は「Slackみたいなモダーンな仕上げに」との要望どおり、名前の頭文字を丸いアイコンにした投稿者アバター、値段・待ち時間・リンクをピル（角丸の小さなラベル）で表示、明細は折りたたみ、というチャットのスレッドに近い見た目にした。

続けて「評価はそれぞれのユーザーができるようにしたい」「今後みんなアカウントをつくってもらって評価できるようにする」との要望。今回は、各小項目に★1〜5の評価をお名前つきで送信できる機能を追加した（同じ名前で再評価すると上書き、平均と件数がその場に表示される）。ただし今回作ったのは、エピソード追加などこれまでと同じ「その場で名前を入力する」方式で、実際のログイン・アカウント機能ではない。「みんながアカウントをつくる」形にするには、誰が誰かをアプリ側で継続的に区別・認証する仕組みが要り、それには今のようなlocalStorageだけで完結する（サーバーを持たない）構成では足りず、ユーザー登録・ログインを扱う本格的なバックエンドが必要になる。これは追記22で保留にした「違うアプリ作ったほうがいいかな」という論点とも重なる話で、アカウント機能を作るタイミングで、この「おもいでWiki」をどこまで今の構成のまま拡張するか、あらためて相談したい。

テストは`test/data.test.js`にStop/StopDetailの並び替え・別行動の複数小項目・評価の上書き/バリデーション・後方互換の変換を追加（14件、計100件）。`test/ui.smoke.js`にも実ブラウザでの一連の操作（大項目の追加→別行動の小項目を2件追加→評価）を追加（14件、計92件）。いずれも全件通過。

この変更はapp.js・index.html・style.cssのみで、Cloudflare Worker（AI深掘り・AIでまとめる）には手を入れていないため、**Workerの再デプロイは不要**（フロントエンドはGitHub Pagesで自動反映）。

## 追記22（開始日時・終了日時・宿泊先の追加）

「開始日時と終了日時を書いてほしい」という要望。追記21で作った`startDate`/`endDate`はinput type=dateで日付のみだったため、input type=datetime-localに変更し、時刻も扱えるようにした。`formatDateJa`/`tripDateLabel`を拡張し、同じ日の中の時間帯（結婚式が14:00〜18:00など）なら日付を繰り返さず時刻だけをつなげて表示するようにした（日をまたぐ場合は両方フルで表示）。既存の日付のみのデータも後方互換で問題なく表示される。

続けて「その旅行でどこ泊まったとか、なにしたかとかもまとめたい」との要望。宿泊先（`lodging`、自由記述）をTripに追加し、同じ編集フォームで入力できるようにした。「なにしたか」はエピソードそのものがすでに担っている記録のため、新しい欄は増やしていない。

あわせて「違うアプリ作ったほうがいいかな」という相談があった。今の拡張はTripという実体にフィールドを足すだけで済む範囲であること、追記14で「データを1箇所にまとめたい」という要望を確認済みであることから、今は分けずに拡張を続けることを提案した（将来Tripだけを切り出したい使い方が増えたら、実体として独立させてあるぶん、そのときのコストは比較的小さいはずと伝えた）。ユーザーの最終判断はまだ。

## 追記21（開始日・終了日をカレンダー入力に／「今日は何の日」）

「開始日と終了日を入れたい。カレンダー的な要素にしたいので」という要望。追記19で追加した`period`（自由記述の時期）は、prompt()ダイアログで入力する簡易なものだったため、Trip（旅行の実体、追記14）に`startDate`/`endDate`（input type=dateで入力する構造化された日付）を追加し、旅行の詳細画面にprompt()ではないちゃんとした編集フォーム（名前・開始日・終了日、キャンセルあり）を作った。年代のまとめ表示・一覧の並び順も、開始日があればそちらを優先するようにした（periodは後方互換のフォールバックとして残す）。

続けて「過去のアルバムからこの日は何していたとか、カレンダーから抽出できたりする？」という要望があったため、「今日は何の日」欄を追加した。今日と同じ月日（年は問わない）に開始日〜終了日が重なる旅行を探して、一覧の上に表示する（年をまたぐ期間にも対応）。タップすると詳細画面に飛べる。

あわせて「写真とかもどれくらいの容量をアップロードできますか？」という質問に、圧縮後のサイズ・localStorageの上限（ブラウザにより5〜10MB程度）・目安の枚数（20〜60枚程度）をREADMEに明記した。

## 追記20（過去のWikiが消えたという報告・loadStoreの安全性を上げる）

ユーザーから「過去のWikiが消えたんだけど、これって保存できるようになっている？」という深刻な報告。

保存の仕組みを再確認：おもいでWikiはサーバーを持たず、その端末のブラウザのlocalStorageだけにすべて保存している（クラウド同期は無い）。そのため、別のブラウザ・別のWindowsユーザー・シークレット/InPrivateウィンドウを開いた場合や、ブラウザの「閲覧データを削除」を行った場合は、その端末のその保存場所が空になり「消えた」ように見える。

あわせてコードを見直したところ、`loadStore()`に見過ごせない不具合を見つけた：保存されている複数のWikiのうち**1件でも`normalizeWiki`の処理中に例外が出ると、try/catchの外側まで例外が伝播し、保存されている全Wikiが「無い」ものとして扱われる**（`emptyStore()`を返してしまう）実装になっていた。実際にこのユーザーのケースがこれで説明できるかは、保存データ自体を見ないと断定できないが、直せる不具合ではあったため、Wikiごとに個別にtry/catchするよう修正した（1件の読み込みに失敗しても、他のWikiは巻き込まれずに残る。失敗した場合はconsole.errorに詳細を出すようにし、原因調査もしやすくした）。

ユーザーには、実際の保存データがブラウザに残っているか（`localStorage.getItem('omoide-wiki:v1')`をDevToolsコンソールで確認）を先に見てもらうよう依頼した。クラウド同期が無い設計のリスクをあらためて明確に伝え、定期的なJSON書き出し（バックアップ）を強く勧めた。

## 追記19（旅行・イベントでまとめる画面に日付と年代別まとめを追加）

ユーザーから「旅行イベントでまとめるのページで日付を出すようにしてほしい。また年代ごとに分けたい。カレンダーのようにしたい」。

「カレンダーのように」を、月表示のカレンダーグリッドではなく、年代（2020年代、など）で区切った時系列の一覧という意味で解釈して実装した（旅行・イベントの記録が月単位で密になることは考えにくく、月カレンダーのマス目よりも年代ごとの見出しの方が実用的だと判断したため）。違えば直せる旨を伝えてある。

- Trip（旅行の実体、追記14）に`period`（時期・自由記述）を追加。エピソード追加フォームの「いつ頃」欄から、その旅行にまだ時期が設定されていなければ自動で補うようにした（新しい入力欄を増やさずに済ませるため）
- `tripYear`/`tripEraLabel`で時期の文字列から西暦4桁を取り出し、「2020年代」のようなラベルに丸める。西暦が読み取れない時期は「時期不明」としてまとめの最後に置く
- 「旅行・イベントでまとめる」の一覧を年代の見出し区切りで表示し、各カードに時期も表示。詳細画面にも時期を表示し、「✏️ 名前を変更」を「✏️ 名前・時期を変更」に拡張して両方直せるようにした（1箇所直せば紐づく全エピソードに反映される、というTripを実体にした際の設計をそのまま活かせた）

## 追記18（AIでまとめる、をエピソードにも広げる／経歴を箇条書きに／基本情報を聞く質問の追加）

ユーザーから3つの要望が続けて来た。

1. 「エピソード部分もAIで整理したけど回答文言が残っているのでそこは整理して」（音声入力の生の書き起こしが、まとめた後もアルバムに残っていた）
2. 「こういうのは文章ではなく箇条書きにしてほしい。あと結婚とか引っ越しの話はこの粒度でいいのか考えてほしい」（生い立ち・経歴が1つの長い文章に圧縮されていて、結婚・転居のような別々の出来事が1文にまとまってしまっていた）
3. 「年齢とか生まれとか血液型とかの基本情報は聞かないの？その年齢を推測して、この年代が好きそうなものを聞いたりとか気を書かせてほしい」（追記12で作った「年代に応じた話題提案」機能が、生年月日を入力する専用画面＝プロフィール表への手入力を前提にしていて、多くの人が埋めないまま使われていなかった）

対応：

- **AIでまとめる、をエピソードにも対応**：これまで`history`/`personality`/`favorites`/`skills`だけが対象で、エピソードのアルバムは常に生の書き起こしのままだった。Workerのcompose機能にエピソード（id・タイトル・本文）を渡し、フィラー（「まあ」「えーっと」）や言い直しを整えた文章をidごとに返してもらう仕組みを追加。エピソードカードにも「元の文章を見る」の折りたたみを追加した
- **生い立ち・経歴だけ箇条書き（年譜）形式に**：Workerのプロンプトで、historyだけ「・」始まりの箇条書き・年代順にし、結婚・引っ越し・転職などの節目は1つの文にまとめず独立した行にするよう指示。フロント側は「・」始まりの行が複数あれば`<ul>`として表示し、それ以外は今まで通り文章として表示するようにした（他のカテゴリは今まで通り文章のまま）
- **生年月日・血液型を聞く質問を追加**：`person.history`に「生年月日を教えてください」「血液型は何ですか？」を追加。プロフィール表（別画面・任意入力）が空でも、インタビューでこの2問に答えていれば、その内容が年代の話題提案に自動的に使われるようにした（`buildProfileContext`でinfoboxと合成）

要Worker再デプロイ。

## 追記17（旧文言リストの取りこぼしを埋める／旅行の詳細画面から直接エピソードを追加）

ユーザーから「また幼稚園について聞かれたぞ」。追記15で「生まれはどこですか？」の旧文言だけを`LEGACY_QUESTION_TEXT`に追加していたが、同じ追記11のタイミングで書き換えた他の`person.history`の質問（幼稚園・小学校・中学校・高校…など）の旧文言は登録していなかった。自分がその場で行った書き換え（Editツールのold_string）は正確に分かる情報なので、その回の書き換えで文言が変わった項目をすべて洗い出し、まとめて登録した（history 11件、personality 2件、favorites 2件を追加。言い回しを変えていない項目は元々ズレないので対象外）。

あわせて「エピソードについては、同じ旅行のエピソードをまとめたいのでデータの持ち方を変えてと言った」という確認。データの持ち方（Trip実体化）自体は追記14で対応済みだったが、**エピソードを追加する入口が旅行の詳細画面には無く**、常にダッシュボードから独立して開く形のままだったため、「旅行の中のエピソード」という階層が保存後の画面遷移には反映されていなかった。旅行の詳細画面に「＋ このイベントにエピソードを追加」を追加し、開くと旅行名が引き継がれた状態でフォームが開き、保存後もダッシュボードではなく同じ旅行の詳細画面に戻るようにした（旅行を選びながら連続してエピソードを足していける）。

## 追記16（AIが生成した質問自体が重複する問題・育てるモードの限界）

ユーザーが68件（うち生い立ち・経歴65件）の記録全体をダッシュボードから貼ってくれた。中に「ご自身のいちばん古い記憶は、どんな場面ですか？」というAI生成の質問が2回出てきており、1回目の回答でユーザー自身が「これさっきも聞かれた気がするな」と書いていた。「集めた記録を見てすでに質問済みだったらしないで。それに派生した質問はしていいけどね」という要望。

これは追記12〜15で対応してきた「固定質問の重複」とは別の問題だった。育てるモード（`growQueueWithAi`）は、AIに渡す文脈として`buildWikiDigest`（各カテゴリ直近5件・1件200文字までに要約したもの）しか渡していなかったため、記録がこれだけ増えると（生い立ち・経歴だけで65件）、直近5件のさらに前に聞いた質問をAIが把握できず、同じ質問を再生成してしまっていた。

対応：

- 「そのカテゴリで既に聞いた質問文の一覧」を返す`askedQuestionTexts(wiki, category)`を追加。回答内容の要約（digest、直近5件・重い）とは別に、質問文そのもの（軽い・短い）は件数を絞らずできるだけ多く渡すようにした（文字数の合計に上限は設けている）
- 育てるモード・通常のAI深掘り追い質問の両方で、このリストをWorkerに渡すようにした
- Worker側のプロンプトに「これらの質問は聞き済みなので繰り返さないこと。ただし自然に派生する新しい角度の質問は歓迎する」という指示を追加（要Worker再デプロイ）

## 追記15（旧文言のまま答えた質問を、既知のものに限りその場で答え済み扱いにする）

ユーザーが実際のダッシュボードの記録を貼ってくれた。「生まれはどこですか？（都道府県・市区町村、当時の様子も分かれば教えてください）」という質問への回答（神奈川県横浜市・横浜市民病院で生まれた、等）と、それを踏まえたAIの深掘り質問4問ぶんの回答が、すでにしっかり残っていた。それにもかかわらず、この質問（元の文言）が再度出てきていた。

原因は追記12で説明した通りだが、今回は「次にスキップを押せば消える」という案内ではなく、実際に画面に貼られた旧文言が判明したため、その場で直接解消できる対応を入れた。

- questionKey導入より前に実際に使われていた「旧文言」を`LEGACY_QUESTION_TEXT`としてキーごとに記録し、`buildInterviewQueue`で「旧文言のまま答えている回答」も答え済みとして扱うようにした
- 今回ユーザーが提示してくれた「生まれはどこですか？」に加えて、今日の言い回し変更（追記11）で書き換えた他の質問（人物像・好きなもの・特技の一部）の旧文言も、自分が直前に書き換えた文言として確実に分かっている範囲であわせて登録した
- 記憶があいまいな古い言い回し（追記2以前など）まではリストに含めていない。それらが再度出てきた場合は、引き続き「一度スキップすれば二度と出ない」（追記13の仕組み）で対応できる

## 追記14（旅行・エピソードの持ち方をTripという実体に変える／domain-modeling Skillの利用）

ユーザーから「エピソード追加は旅行の中のエピソードだから、階層としては『〇〇旅行のこのエピソード』になる。そのデータの持ち方をして」という指摘。これまでは`episode.trip`という自由記述の文字列で、同じ文字列のエピソード同士をその場でグルーピングしているだけだった（表記ゆれで同じ旅行が別グループに分かれる、名前を直すのに全エピソードを個別編集する必要がある、エピソードが1件もない旅行を先に作れない、という実害があった）。

`domain-modeling` Skillを使って設計判断を整理し、`apps/day18-omoide-wiki/CONTEXT.md`（用語集）と`apps/day18-omoide-wiki/docs/adr/0001-trip-as-entity.md`（決定の記録）を初めて作成した。

対応：

- `Trip`を`{id, title, createdAt, updatedAt}`を持つ実体にし、`wiki.trips`で保持。`episode.trip`（文字列）を`episode.tripId`（参照）に置き換えた
- エピソード追加フォームのUIは変えず（自由記述の入力欄のまま）、`<datalist>`で既存の旅行名を補完するのみに留めた。保存時に`findOrCreateTrip`で名前が一致する既存Tripを探すか、無ければ新規作成する（UIを複雑にせず、データの持ち方だけを直す）
- 旧形式（`episode.trip`文字列のみ）のWikiは`normalizeWiki`で読み込み時に自動移行する
- Tripが実体になったことで、「旅行・イベントでまとめる」の詳細画面に「✏️ 名前を変更」を追加。1箇所直すだけで紐づく全エピソードに反映される（文字列マッチ時代はできなかったこと）

## 追記13（既存Wikiで質問が繰り返される不具合／人生の節目の質問／写真添付）

ユーザーから「清水皓也で質問がすでに回答しているものが何度も質問される」「人物像しか質問しない？」という報告。

原因の切り分け：追記12で質問の重複防止をquestionKeyベースに変えたが、それより前（追記11など）に答えられた記録は、答えた時点の文言がすでに何度も書き換わっていて、現在の質問文言ともquestionKeyとも一致しない（questionKey自体が導入前で存在しない）。そのため「答え済み」と認識されず、特に書き換え回数が多かった人物像カテゴリの質問がまとまって再度キューに並び、さらに1問につきAI深掘りが最大6往復続くため、体感として「人物像ばかり延々と聞かれる」状態になっていた。

これは言い回しを直すたびに起きうる問題なので、今回は場当たり的な回避ではなく、「一度スキップした質問は二度と出さない」という仕組みを追加した。

- 「この質問はとばす」を押した質問のkeyを `wiki.skippedKeys` に永続的に記録し、答えていなくてもインタビュー再開時に除外するようにした（`buildInterviewQueue` に反映、複数人JSON合体時も和集合でマージ）
- これにより、今回のように文言変更で答え済み判定が効かなくなった質問が出てきても、ユーザーが一度「とばす」を押すだけで完全に消せるようになった

あわせて、ユーザーから「祖父母がボケる前に、色々な人生経験をしていたと50年後にも思えるものを残したい。結婚・引っ越しなどのライフイベントも」「旅行はどこに行ったか」という要望。

- 生い立ちカテゴリに結婚・子供・引っ越し・退職の質問を追加（既存の質問キー方式のおかげで、追加しても既存の回答には影響しない）
- 旅行のエピソード質問を「どこに行ったか」を明示的に聞く言い回しに変更
- Workerの追い質問プロンプトに「あとで家族が読み返したときに豊かな人生だったと実感できる記録にする」という目的を明記し、具体的な場面（いつ・どこで・誰と）を優先するよう指示（要Worker再デプロイ）

さらに「写真も残したいが、難しい処理はさせたくない」という要望を受け、インタビューの回答にも任意で写真を添えられるようにした。エピソード追加で使っている自動圧縮の仕組み（`fileToCompressedDataURL`）をそのまま使い回し、新しい処理は増やしていない。ダッシュボードの記録一覧・完成ページの元の回答表示の両方にサムネイルを出す。

## 追記12（旅行・イベント単位でまとめる画面／質問の重複防止をキー化／年代に応じた話題提案）

ユーザーから複数の要望が続けて来た。

1. 「Wikiというより、この旅行はだれがいてどんなエピソードがあって…というのをまとめたい。みんなの思い出日記的な立ち位置かな」
2. 「その性格が一番はっきり出た、具体的な出来事を一つ教えてください、という質問の意図が分からない」
3. 「デザインが暗い色基調になっているので明るい色基調に変えてほしい」
4. 「一度聞いたものは把握して同じ質問をしないでほしい」
5. 「年代を把握して『これ好きじゃなかったですか』と提案してほしい（平成7年生まれならヘキサゴン・プロポーズ大作戦・嵐・SMAPなど、流行りを具体的に挙げて聞く）」

対応：

**旅行・イベントでまとめる画面**：人物・サークルの「Wikipedia」ページとは別に、ダッシュボードから「旅行・イベントでまとめる」を開けるようにした。一覧では「旅行・イベント名」をつけたエピソードが旅行単位でまとまり、参加者（エピソードを書いた人＋新設した「その場にいた人」欄）・写真・件数が並ぶ。選ぶと、その旅行のエピソードを時系列・写真つきで見返せる。既存の `groupEpisodesByTrip`（DAY19序盤に実装）をそのまま再利用し、新しい概念を増やさずに済んだ。

**質問の重複防止をキー化**：「その性格が一番はっきり出た〜」という質問は、直前にどんな性格かを一度も聞いていないのに「その」と参照していた「宙に浮いた指示語」バグだった（追記11で修正済み）。ただしこの手の言い回し調整は今後も起こりうるため、これまでの「質問文の完全一致」で重複を防ぐ仕組みだと、言い回しを変えるたびに既に答えた質問が再度出てきてしまう。そこで質問バンク（`QUESTIONS`）を `{key, text}` の形に変え、重複判定を「questionKeyの一致」に変更（questionKeyが無い古い記録は本文一致でフォールバック、後方互換）。今後は言い回しを変えても、一度答えた質問が再び聞かれることはない。

**配色をライトテーマに変更**：色がすべてCSS変数で管理されていたため、`:root` のトークンを差し替えるだけで対応（完成ページはもとから白背景なので変更なし）。

**年代に応じた話題提案**：プロフィール表（生年月日・結成年など）をAIの追い質問プロンプトに渡すようにし、深掘りの観点に「生まれ年・結成年から分かる時代の流行（テレビ番組・音楽・芸能人など）を具体的に挙げて、好きだったか尋ねる」を追加。年代が確信を持てない場合は無理に使わないよう明記した（誤った時代のものを挙げると逆効果なため）。要Worker再デプロイ。

いずれもテスト（データ・UIスモーク）を追加・更新し、実ブラウザのスクリーンショットで見た目も確認した。

## 追記11（質問の雰囲気・深掘りの判断基準・前の質問に戻る）

ユーザーから4つの要望。

1. 「質問はもっと回答側がウキウキ回答できる感じにしてほしい」
2. 「もっと深掘ってほしいことは前の質問の回答の多さとかで判断してほしい」
3. 「あなたはプロのインタビュワーとしてその人の人生を深くもっともっと知っていくような立ち位置です」
4. 「もっと趣味とか特技も聞いてほしい」

対応：

- 固定質問（`QUESTIONS`）の言い回しを見直し、「ぜひ聞かせてください！」「教えてください！」のような明るく前向きな表現を増やした。趣味・特技に関する質問も追加（好きな趣味・没頭できること、特技を始めたきっかけなど）
- Workerの追い質問プロンプトのペルソナを「話し上手な友人」から「その人の人生をもっともっと深く知っていきたい、プロのインタビュアー」に変更し、答える側がウキウキ・ワクワクする明るい聞き方をするよう指示を追加（要Worker再デプロイ）
- 深掘りを続けるかどうかの判断基準に「直前の回答の文字数・具体性」を明示的に追加。回答が短く情報が薄い場合は無理に掘らず、回答が具体的でエピソードが豊富な場合は積極的に掘り続けるようプロンプトに指示した（回答の文字数も明示的に渡すようにした）
- 深掘りの観点（ANGLES）に「趣味・特技についてまだ聞けていない話」を追加

あわせて、インタビュー画面に「前の質問に戻る」ボタンを追加した。直前の回答をいったん取り消し（保存済みの記録からも削除）、回答欄に戻して書き直せるようにする設計（戻る＝取り消して答え直す、という単純なモデルにして、複雑な編集画面を増やさないようにした）。UIスモークテストにも検証を追加した。

さらにユーザーから「その性格が一番はっきり出た、具体的な出来事を一つ教えてください、という質問の意図が分からない」という指摘。原因は、「その性格」が指す中身（本人がどんな性格だと思っているか）が、それより前のどの質問でも一度も聞かれていないまま「その」と参照していたこと（宙に浮いた指示語）。同じ書き方のミスが `personality`・`favorites`・`skills` の複数の質問（person・group 両方）に残っていたため、該当する質問をすべて洗い出し、「名付ける部分」と「エピソードを聞く部分」を1つの質問の中で完結させる形に書き直した（例：「その性格が〜」→「自分の性格をひとことで言うと、どんな感じだと思いますか？そう思うきっかけになった具体的な出来事も、あわせて聞かせてください！」）。単発の誤字修正ではなく、同じパターンの質問を横断的に点検して直した。

デザインについても「暗い色基調になっているので明るい色基調に変えてほしい」との要望。色はすべてCSS変数（`--bg`・`--panel`・`--text` など）で管理されていたため、`:root` のトークン値をダーク配色からライト配色（白basedの背景・濃紺文字）に差し替えるだけで済んだ。完成ページ（Wikipedia風）はもともと白背景で作っていたため変更していない。実ブラウザでスクリーンショットを撮って見た目を確認した。

## 追記10（AIでまとめるがタイムアウトする）

改善した理由表示のおかげで、今度は原因がすぐ分かった：「時間切れ（応答に時間がかかりすぎました）」。記録が増えて（濃い内容の回答も多く）、まとめる文章量が増えた分、25秒のタイムアウトを超えるようになっていた。

- フロント側のタイムアウトを25秒→60秒に延長
- Workerのcompose側のreasoning effortを medium→low に下げた（まとめ直しは書き直し寄りの作業で、追い質問ほど深い推論は不要なため。要Worker再デプロイ）

記録が今後さらに増えると、また時間がかかるようになる可能性はある。そのときは分割してまとめる（カテゴリごとに別リクエストにする等）方式への変更を検討する。

## 追記9（会話のトーンと、休憩を挟める仕組み）

ユーザーから2つの要望。

1. 「質問がちょっとナチュラルではない。なんか会話している感じにしたい」
2. 「質問が多くて、おばあちゃんにしゃべらすと疲れちゃうからこまめに止めたい。何日にも分けてヒアリングする感じにしたい」

②で分かったのは、このアプリは実際にユーザーの祖母にインタビューするために使われている、ということ。①②はどちらも「音声で自然に会話する」という当初の目的に立ち返る内容だった。

対応：

- Workerの追い質問プロンプトを、「質問文だけを機械的に返す」から「直前の回答への短い相づち（『それは大変でしたね』など）を添えてから、雑談のような自然な話し言葉で聞く」方式に変更（要Worker再デプロイ）
- インタビュー画面に「5問ごとの休憩確認」を追加。`confirm()`で「続ける／今日はここまでにする」を聞き、後者を選んでもそれまでの回答はすべて保存済みなので、日をまたいで続きから聞ける（既存の「同じ質問を繰り返さない」仕組みと組み合わさる）

「質問100個でもいい」（追記5）と「こまめに止めたい」は一見矛盾するが、前者は蓄積される記録の総量、後者は1回のセッションの長さの話なので、両立するように設計した（何日にも分けて積み上げれば、結果的に100問にも到達できる）。

## 追記8（バグ修正・AIでまとめるが失敗する）

デプロイ後、ユーザーから「完成ページがまとめられていない」「AIでまとめるボタンが機能していない」と報告。原因の切り分けのため、失敗理由（レート制限／サーバーエラー／タイムアウト／通信エラー）をアラートに表示するようにした。あわせて、compose用の件数・文字数の上限が「質問100個でもいい」という以前の要望と矛盾して低すぎた（1カテゴリ30件、1件2000文字まで）ため、150件・4000文字まで引き上げ、モデルへ渡す総文字数にも上限（24,000文字）を設けて費用が青天井にならないようにした。

## 追記6（公開後のフィードバック対応・回答をAIが文章としてまとめる）

「入力されたものをAIでまとめて出力してほしい」に対応。これまでは一問一答の生の回答がそのまま完成ページに箇条書きで出ていたが、それをWikipedia記事のような自然な文章に書き直す機能を追加した。

- Workerに新しい仕組みは増やさず、同じ`omoide-wiki-ai` Workerに `action: "compose"` を付けたときだけ別の処理をする形で追加（新しいWorkerの公開・secret登録は不要。既存のWorkerを再デプロイするだけでよい）
- 「事実を創作しない」「三人称のWikipedia文体に整える」ことを厳守するよう指示し、生成後も各項目に「元の回答を見る」を残して、いつでも元のやり取りを確認できるようにした（AIの要約だけが残って、本人が実際に何と答えたかが失われることのないように）
- 記録が無い項目や、まとめが空で返ってきた項目は、これまでどおり一問一答のまま表示する

## 追記5（公開後のフィードバック対応・インタビューを「育てる」ようにする）

ユーザーが実際にデプロイして使ってみたところ、立て続けに要望が来た。

- 「入力されたものをAIでまとめて出力してほしい」（次のタスクとして着手予定、今回は未着手）
- 「質問を一回やめると同じ質問が来る」「異論なく（＝いろんな角度から）Wikiを育てるように質問してほしい」
- 「入力されたデータからどんどん話を引き出すヒアリングマスターのような形で質問してほしい」
- 「高校の仲良かった人、サークルが同じ人、バイト先の人などどんどん深掘って引き出してほしい」
- 「定型文しか質問ないじゃん」
- 「好きな歌、なぜ好きか、1年ごとに細かく、そのときの感情をもっと聞いてほしい」
- 「質問100個してもいいので、人生が詰まった資料にしたい」
- 「老人でもできるように対話式で、話題が終わったら『次』と言えば次に行けるようにしたい」

対応した内容：

1. **同じ質問を繰り返さない**：`buildInterviewQueue` に現在のWikiを渡し、既に回答済み（`prompt` が一致する）固定質問はキューから除くようにした
2. **育てるモード**：固定質問を全部使い切ったら、そこで終わらずに「ヒアリングマスターとして、ここまでの内容全体（概要＋各カテゴリの直近の回答）を踏まえ、まだ聞けていない話を引き出す質問」をAIに考えてもらい、インタビューを続ける。年代・時期を絞って深く聞くことも歓迎する旨を明示し、「1年ごとに細かく」という要望にも対応できるようにした
3. **質問の中身を強化**：高校の友人、大学のサークル・部活の友人、アルバイト先の人間関係を聞く質問を追加。好きな曲とそのきっかけを聞く質問を追加。進学・就職先について「良かったこと・つらかったこと」も聞くようにした
4. **Workerの深掘りの観点にも「一緒にいた友人・仲間」「良かったこと・つらかったこと」を追加**（要Worker再デプロイ）
5. **声だけで完結できるように**：音声で答えているとき、話し終わりに「次」と言うと、ボタンを押さなくても自動的に記録して次の質問に進むようにした（お年寄りなど、画面操作が難しい人でも対話だけで進められるように）

上限（1話題6往復）やレート制限は変えていない。「100問でもいい」という要望には、育てるモードが理論上いくらでも続けられることで応えている（本人が「← やめる」で止めるまで続けられる）。

「AIでまとめて出力してほしい」（回答をそのまま並べるのではなく、AIが文章として整えて完成ページに出す）は、Workerに新しいエンドポイントが要る規模の変更のため、次の作業として着手する。

## 追記4（公開後のフィードバック対応・AI深掘りをしっかり効かせる）

ユーザーから「深掘りするときはAIを使ってしっかり深堀って聞いてほしい、お金かかってもいいので」「ある程度聞くものは決めておいてもらって、そこからネタになりそうなものを探す感じにして」という要望がありました。

- 1話題あたりの深掘り上限を3→6往復に引き上げ
- Workerのモデル設定を reasoning effort "minimal" → "medium" に変更し、質問の質を優先
- Workerのプロンプトを、完全に自由な深掘りから「あらかじめ決めた観点（日時・場所、一緒にいた人、そのときの気持ち、きっかけ、印象的な言葉、その後の変化、細かい情景、類似の出来事）の中から、今の回答にとって一番ネタになりそうなものを選んで質問する」方式に変更
- Workerを公開済みなら「AIで深掘りする」トグルを初回からONにし、次回以降もその選択を記憶するようにした（音声トグルと同じ仕組み）

「費用がかかってもいい」という前提を明示的にもらえたので、費用より深掘りの質・量を優先する設定に振っている。ただしクライアント側の上限（6往復）とレート制限は、誤操作やバグによる暴走を防ぐ安全弁として残した。

## 追記3（公開後のフィードバック対応・生い立ちを年代順に聞く）

ユーザーから実際のWikipedia記事（山下智久さんの記事全文）が送られてきて、「生まれはどこ、幼稚園はどこ、小学校はどこ、と舐めるように質問してほしい」という要望がありました。実在のWikipedia記事の「来歴」節が、具体的な出来事の積み重ねで年代順に書かれていることを踏まえ、対応しました。

- 新しいカテゴリ「生い立ち・経歴」（person）／「沿革」（group）を追加し、質問の一番手前に配置
- personは「生まれた場所」→「幼稚園」→「小学校」→「中学校」→「高校」→「大学・就職」→「人生の転機」まで、10問を年代順に用意
- groupは結成の経緯・名前の由来・活動の変遷など7問
- 完成ページの各セクションで、答えた質問文を薄い文字で表示するようにした（特に生い立ちセクションは、質問文がないと年代の脈絡が読み取れないため）
- 既存のWiki（historyカテゴリが無い状態で保存されたもの）を読み込んでも落ちないよう、読み込み時に配列を補う関数（normalizeWiki）を追加した

## 追記2（公開後のフィードバック対応・デザインと質問の言い回し）

ユーザーから2つのフィードバックがありました。

1. 「実在の有名人のWikipedia記事のような、具体的なエピソードが読みたい。今の質問は性格を抽象的に聞いているだけ」
2. 「完成ページのデザインがダサい。もっとスタイリッシュにできないか」

対応：

- 固定質問（`QUESTIONS`）を全カテゴリぶん書き直し、「性格は？」のような抽象的な聞き方をやめて、**「その性格が一番はっきり出た、具体的な出来事を一つ教えてください」のように実際の場面を聞く**言い回しに統一した
- 完成ページのCSS・HTML構成を作り直し、**実際のWikipedia記事の配色（白背景・黒に近い本文・青リンク）・書体（見出しは明朝、本文はゴシック）・構成（目次box・infobox内に写真と名前・下部のカテゴリ欄）**に寄せた。これまでは独自の「セピア色の紙」風デザインだったが、ユーザーが実例として挙げた山下智久さんのWikipedia記事に近い見た目に変更
- アプリ全体のアクセントカラーも、暖色系のオレンジから深い藍色（インディゴ）に変更し、落ち着いた印象にした

## 追記（公開後のフィードバック対応）

`main` へマージして公開した直後、ユーザーから「質問も音声でほしい」とフィードバックがありました。機能自体（質問の読み上げ）はすでにあったのですが、インタビュー画面を開くたびに「音声で会話する」トグルが強制的にOFFへ戻る作りだったため、毎回オンにし直す必要がありました。これは想定した使い方（スピーディーに音声で答え続ける）と噛み合っていなかったため、**初回はON・以降は端末に選択を記憶する**方式に直しました。「機能はあるのに使われない」は、実装漏れと同じくらい実害があると分かった一件です。

## 今日の動画で使えるポイント

1. ヒアリングの途中でユーザーから次々と新しい要望（サークル旅行、車内録音）が出てきて、話が大きくなっていく場面。`interview-me` Skillの「一問ずつ・確定してから進む」型が、話を広げたまま実装に入ってしまう事故を防いだ
2. 「AIによる深掘り」は一見すると新しい画面や仕組みが要りそうに見えるが、既存のインタビューの質問キューに動的な質問を差し込むだけで実装できた。大きく見える機能ほど、既存の仕組みに乗せられないか先に考える価値がある
3. 自前で立てたテスト用のフェイクサーバーが、本番のWorkerと同じCORS制約を受けることを忘れていて、原因不明のタイムアウトにハマった。「本番と同じ制約を受けているか」をテスト用の代用品にも適用する必要がある、という気づき

## DAY RESULT

```
DAY 19 RESULT
アプリ：おもいでWiki（ブラッシュアップ）
人間：0h（自動実行セッション）
AI：正確な計測不可
費用：0円（Worker未デプロイのため今回のAPI実費は発生せず）
今日できるようになったこと
・interview-me Skillで要件を1つずつ確定させてから実装する
・AIの深掘り質問を、既存のインタビュー画面にそのまま差し込む設計
つまずいたこと
・テスト用フェイクサーバーのCORS設定漏れでタイムアウト
あと11 apps
```
