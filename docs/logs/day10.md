# DAY 10：ドラマ王（ブラッシュアップ）

**新しい挑戦**：Skills でAI自体をレベルアップする。

ロードマップ上の DAY10 は「くじ引きリメイク」でしたが、**題材をドラマ王（DAY5）のブラッシュアップに変更**しました。新しい挑戦「Skills でAI自体をレベルアップ」はそのままです。既に動いているアプリを Skills で磨き直すほうが、Skills を入れる前と後の差が見えやすいためです。

DAY7（サッカー出欠管理）・DAY8（草野球チーム管理）・DAY9（SNS投稿管理）は未着手のまま残っています。順番を飛ばしています。

## 時間

| | |
|---|---|
| 開発開始 | 13:09（JST 2026-08-29） |
| 開発終了 | 13:43 |
| 人間作業 | 正確な計測不可 |
| AI実稼働 | 正確な計測不可 |
| AI待機 | 正確な計測不可 |

実時間で約30分。人間の入力待ちとAIの実稼働を分けて計測していないため、内訳は取れていません。

## 使用モデル

| モデル | 作業内容 |
|---|---|
| Claude Opus 5（Claude Code） | Skill の作成、アプリの改修、テスト、記録 |

アプリの中から呼ぶモデルは画面で選べるようにしました（既定 Opus 5、ほかに Sonnet 5・Haiku 4.5）。

## 費用

| 項目 | 金額 |
|---|---|
| AI利用料金 | 定額プランの範囲内（追加の請求なし） |
| API料金 | 0ドル |
| その他 | 0円 |

**API料金が0ドルなのは、開発中に一度も本物の API を呼んでいないからです。** テストでは Anthropic への通信を差し替えているので、キーも費用も要りませんでした。実際に遊ぶときは1回の出題でおよそ $0.12（Opus 5）かかり、それは遊ぶ人自身の鍵に課金されます。

## 実施作業

**1. 出題設計を Skill として書き出した**（`.claude/skills/quiz-design/`）

クイズの良し悪しをどう判断するかを文章にしました。芯は「**クイズの質は、正解ではなく間違いの選択肢で決まる**」ということ。出題を出す前の4つの検査（正解が一意か／ダミーが正解と同じ見た目の階層か／消去法で潰せないか／答えが問題文に漏れていないか）と、ダミーの取り方、難易度の層の持ち方、AIに問題を作らせるときの作法を書いています。検査を機械で回すスクリプトも付けました。

**2. その Skill をドラマ王に当てた**

当てた結果、自分で作ったアプリの中から2つ見つかりました。

- **同じ言い回しの設問が4問並んでいた。** 「『あまちゃん』で ○○ を演じたのは？」が結果画面に4つ。README には「系統ごとに1問まで」と書いてあったのに、コードは1テンプレから4問作れる状態でした。書いたつもりと実物がずれていた
- **結果画面の内訳の帯が、一度も伸びていなかった。** `span` のままで `height` が効かず、ずっと空の帯を出していた。気づいていませんでした

**3. AIに問題を作らせる仕組みを足した**

DAY5 の時点では、記事の表と箱書きにある事実しか問えませんでした。「誰が何を演じたか」は問えても「物語の中で何が起きたか」は問えない。そこで記事の文章（あらすじ・登場人物の節）を Claude に読ませて、中身を問う設問を作らせるようにしました。

大事なのは**AIの出力をそのまま出さないこと**です。返ってきた設問を上の4検査に通し、落ちたものは捨てます。捨てたぶんは今までのテンプレート出題で埋めるので、AIが1問も通らなくても、鍵が無くても、通信に失敗しても遊べます。

**4. APIキーの扱いを決めた**

キーはリポジトリに置かず、遊ぶ人が画面から入れてその端末の中にだけ保存する形にしました。作った人のキーを埋め込んで配ると、他人が遊んだぶんまで自分の請求になります。かかった額は返り値から計算して結果画面に出します（ドルのまま。換算しない）。

## エラー・つまずき

- **作業ブランチの初期位置が古かった。** DAY3（チンチロ）のコミットを指していて、DAY6 で整理した `apps/` の形になっていなかった
- **公式SDKが使えなかった。** Claude の公式SDKは npm パッケージで、ビルド無しの1枚HTMLには置き場所がない。CDN から読む手もあるが、第三者への実行時依存が増え、ファイルを直接開いたときに動かないことがある
- **自分で書いた検査が、正しい設問を弾いた。** 「正解だけ長さが浮いている」の判定が比率だけを見ていたため、「海女」対「喫茶店の店主」のような日本語では普通の組み合わせまで落としていた。実際にAIが作った良い設問が1問消えていた
- **繰り返しを厳しく禁じたら、今度は問題数が足りなくなった。** 上限をきつくすると、材料の薄い記事で10問に届かない

## 解決方法

- ブランチは `main` から切り直した（`git checkout -B <ブランチ名> origin/main`）
- SDKは使わず、素の `fetch` で API を呼んだ。ブラウザから直接呼ぶのに要るヘッダは、公式SDKの実装を読んで正確な名前を確かめてから自分で付けた
- 長さの判定は、比率だけでなく字数の差も一緒に見るようにした。**Skill 側の検査スクリプトも同じ基準に直した**（基準が2箇所に分かれると必ず食い違うため）
- 上限は「まず変化をつけて採り、足りないときだけ控えから足す」二段構えにした。問題が少ないことと繰り返しに見えることを比べて、少ないほうが困ると判断した

## 今日できるようになったこと

- 判断の基準を Skill として書き出し、AIに毎回同じ基準で作らせる
- 書き出した基準を自分の作ったものに当てて、見えていなかった不具合を見つける
- アプリの中からAIを呼んで、中身を考えさせる
- AIの出力を鵜呑みにせず、機械で検査して落ちたぶんを捨てる
- 他人に配るアプリで、APIキーを埋め込まずに使う人自身の鍵で払わせる
- 本物のAPIを呼ばずに、AIを使う機能をテストする

## 今日の動画で使えるポイント

1. **Skill を当てたら、自分のアプリのバグが2つ出てきた。** 「同じ問題が4回並ぶ」と「グラフが一度も伸びていなかった」。しかも README には正しいことが書いてあって、コードだけがずれていた。**書いたつもりと実物のズレ**が、基準を外に出したことで見えた
2. **AIに問題を作らせても、そのまま出さない。** 4つの検査に通して落ちたぶんは捨てる。今日も実際に「答えが選択肢に無い」問題が返ってきて、それは表に出ずに捨てられた
3. **他人に配るアプリでAPIキーをどうするか。** 自分の鍵を埋め込むと他人の遊んだ分が全部自分の請求になる。だから遊ぶ人に自分の鍵を入れてもらう。そして**いくらかかったかを画面に出す**。見えないまま課金される作りにしない

## DAY RESULT

```
DAY 10 RESULT
アプリ：ドラマ王（ブラッシュアップ）
人間：正確な計測不可（実時間で約30分）
AI：正確な計測不可
費用：0円（API呼び出しは0ドル）
今日できるようになったこと
・判断の基準を Skill に書き出して、AIに同じ基準で作らせる
・アプリの中からAIを呼び、その出力を検査してから使う
つまずいたこと
・自分で書いた検査が、正しい設問まで弾いた
・繰り返しを厳しく禁じたら、問題数が足りなくなった
あと20 apps
```
