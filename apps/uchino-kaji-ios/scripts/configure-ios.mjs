import fs from 'node:fs/promises';import path from 'node:path';import plist from 'plist';import xcode from 'xcode';import sharp from 'sharp';
const infoPath='ios/App/App/Info.plist';const info=plist.parse(await fs.readFile(infoPath,'utf8'));info.CFBundleDisplayName='うちの家事';info.ITSAppUsesNonExemptEncryption=false;await fs.writeFile(infoPath,plist.build(info));
const project=xcode.project('ios/App/App.xcodeproj/project.pbxproj');project.parseSync();const version=JSON.parse(await fs.readFile('package.json','utf8')).version;const build=process.env.BUILD_NUMBER||'1';if(!Number.isInteger(Number(build))||Number(build)<1)throw new Error('BUILD_NUMBER must be numeric');project.updateBuildProperty('MARKETING_VERSION',version);project.updateBuildProperty('CURRENT_PROJECT_VERSION',build);if(process.env.IOS_TEAM_ID&&process.env.IOS_PROFILE_UUID){project.updateBuildProperty('DEVELOPMENT_TEAM',process.env.IOS_TEAM_ID);project.updateBuildProperty('CODE_SIGN_STYLE','Manual');project.updateBuildProperty('CODE_SIGN_IDENTITY','"Apple Distribution"');project.updateBuildProperty('PROVISIONING_PROFILE_SPECIFIER',process.env.IOS_PROFILE_UUID);}
await fs.copyFile('resources/PrivacyInfo.xcprivacy','ios/App/App/PrivacyInfo.xcprivacy');if(!project.pbxGroupByName('Resources')){const group=project.addPbxGroup([],'Resources');const main=project.getFirstProject().firstProject.mainGroup;project.getPBXGroupByKey(main).children.push({value:group.uuid,comment:'Resources'});}delete project.pbxGroupByName('Resources').path;if(!project.hasFile('App/PrivacyInfo.xcprivacy'))project.addResourceFile('App/PrivacyInfo.xcprivacy',{target:project.getFirstTarget().uuid});
const icons='ios/App/App/Assets.xcassets/AppIcon.appiconset';await fs.mkdir(icons,{recursive:true});await sharp('resources/icon.svg').png().toFile(path.join(icons,'AppIcon.png'));await fs.writeFile(path.join(icons,'Contents.json'),JSON.stringify({images:[{filename:'AppIcon.png',idiom:'universal',platform:'ios',size:'1024x1024'}],info:{author:'xcode',version:1}},null,2));await fs.writeFile('ios/App/App.xcodeproj/project.pbxproj',project.writeSync());console.log('Configured iOS identity, version, privacy manifest and icon.');
