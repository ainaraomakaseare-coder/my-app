# This file is a template. Copy to .env locally; never commit the real key.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-4o-mini
# Set once an actual public app or App Store URL exists. Never use localhost.
PUBLIC_APP_URL=
PORT=60154
